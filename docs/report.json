{
  "tool": {
    "name": "SolGuardian",
    "version": "0.1.0",
    "mode": "static-heuristic (AST-lite + pattern rules). No ML, no network, no RPC.",
    "skills_dir": "skills/"
  },
  "target": {
    "root": "samples",
    "kind": "mixed",
    "files": [
      "clean/CleanVault.sol",
      "clean/clean_vault.rs",
      "solana/vault/programs/vault/src/lib.rs",
      "solidity/HyperVault.sol",
      "solidity/Vault.sol"
    ],
    "lines_scanned": 935
  },
  "detectors_run": [
    "evm_access",
    "evm_delegatecall",
    "evm_external_calls",
    "evm_oracle",
    "evm_reentrancy",
    "evm_selfdestruct",
    "evm_sig_replay",
    "solana_accounts",
    "solana_authority",
    "solana_cpi",
    "solana_deser",
    "solana_math",
    "solana_signer"
  ],
  "summary": {
    "total": 49,
    "duration_ms": 669,
    "by_severity": {
      "critical": 16,
      "high": 18,
      "medium": 12,
      "low": 3,
      "info": 0
    },
    "by_chain": {
      "hyperevm": 14,
      "solana": 25,
      "evm": 10
    },
    "by_detector": {
      "solana_signer": 6,
      "solana_accounts": 6,
      "evm_access": 5,
      "solana_math": 5,
      "evm_sig_replay": 4,
      "evm_selfdestruct": 4,
      "solana_cpi": 4,
      "evm_reentrancy": 3,
      "evm_oracle": 3,
      "evm_external_calls": 3,
      "solana_authority": 3,
      "evm_delegatecall": 2,
      "solana_deser": 1
    },
    "corroborated": 30
  },
  "findings": [
    {
      "id": "SG-EVM-001",
      "detector": "evm_delegatecall",
      "rule": "DC-001",
      "title": "delegatecall to unvalidated target in migrate()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.92,
      "score": 8.712,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 54,
        "end_line": 54,
        "function": "migrate",
        "contract": "HyperVault"
      },
      "description": "migrate() executes `target.delegatecall(...)`. the delegatecall target `target` is an instruction argument, so any caller chooses the code that runs inside this contract's storage. delegatecall keeps this contract's storage and msg.sender, so the callee can rewrite owner/admin/implementation slots, mint, drain, or call selfdestruct. Authorisation on the wrapper (absent) does not help if the target itself is attacker-chosen.",
      "evidence": "(bool ok, ) = target.delegatecall(data);",
      "exploit_sketch": [
        "Deploy a malicious implementation contract.",
        "Call migrate() with your contract as the target (or first move the pointer via the unguarded setter).",
        "Inside the callee, write the admin/owner slot, then use the admin path to take the funds."
      ],
      "patch_sketch": [
        "Only delegatecall to a constant, admin-upgraded-with-timelock implementation address.",
        "Add EIP-1967 slots + an `onlyProxyAdmin` + timelock upgrade path.",
        "Validate `target.code.length > 0` and that the target is in an allowlist."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-001_sg_evm_001_delegatecall_to_unval.t.sol",
        "how_to_run": "forge test --match-contract SgEvm001DelegatecallToUnvalidatedTargetIHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-001 - delegatecall to unvalidated target in migrate()\n// Severity: critical (confidence 0.92)\n// Rule    : DC-001\n// Skill   : skills/delegatecall-proxy/SKILL.md\n// Location: solidity/HyperVault.sol:54 (migrate)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm001DelegatecallToUnvalidatedTargetIHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm001DelegatecallToUnvalidatedTargetIHarness\n/// @notice PoC harness for SG-EVM-001.\ncontract SgEvm001DelegatecallToUnvalidatedTargetIHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice delegatecall to unvalidated target in migrate()\n    function test_sg_evm_001_delegatecall_to_unvalidated_target_in() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Deploy a malicious implementation contract.\n        // Step 2 - Call migrate() with your contract as the target (or first move the pointer via the unguarded setter).\n        // Step 3 - Inside the callee, write the admin/owner slot, then use the admin path to take the funds.\n\n        vm.prank(attacker);\n        // TODO: call HyperVault.migrate(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_001_delegatecall_to_unvalidated_target_in_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Only delegatecall to a constant, admin-upgraded-with-timelock implementation address.\n        //   Add EIP-1967 slots + an `onlyProxyAdmin` + timelock upgrade path.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/delegatecall-proxy/SKILL.md",
      "skill_checklist": [
        "The delegatecall target resolves to a constant or admin-upgraded implementation address -",
        "Writes to logic pointers are guarded by an admin role **and** a timelock/delay, and emit an"
      ],
      "cwe": "CWE-471",
      "tags": [
        "delegatecall",
        "upgradeability",
        "DC-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-001",
      "detector": "solana_deser",
      "rule": "DESER-001",
      "title": "Unsafe cast of account data into `VaultConfig` in load_foreign_config()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.9,
      "score": 8.64,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 117,
        "end_line": 117,
        "function": "load_foreign_config",
        "contract": ""
      },
      "description": "load_foreign_config() reinterprets borrowed account bytes as `*const VaultConfig` and dereferences it. The layout of an account is entirely caller-supplied: there is no owner check and no length/discriminator check here, so the program reads memory it never validated. That is a type-conflict / out-of-bounds read on a chain where the runtime will not save you - and even when it only reads garbage, the value becomes trusted state.",
      "evidence": "let cfg: &VaultConfig = unsafe { &*(data.as_ptr() as *const VaultConfig) };",
      "exploit_sketch": [
        "Create an account with the program's own PDA-looking address (or any short account) and pass it in.",
        "The cast reads whatever bytes exist - attacker-chosen numbers become `limiter`, authority, etc.",
        "Use the forged field to unlock the privileged branch of the handler."
      ],
      "patch_sketch": [
        "Use Anchor's typed account (`Account<'info, VaultConfig>`), which verifies owner + discriminator.",
        "If raw access is unavoidable: check `account.owner == &id()`, then `VaultConfig::try_deserialize(&mut &data[8..])` and never dereference a cast.",
        "Add `require!(data.len() >= 8 + size_of::<VaultConfig>(), ...)` before reading."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-001_sg_sol_001_unsafe_cast_of_accoun.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_001_unsafe_cast_of_account_data_into_vaul",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-001 - Unsafe cast of account data into `VaultConfig` in load_foreign_config()\n// Severity: critical (confidence 0.90)\n// Rule    : DESER-001\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:117 (load_foreign_config)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_001_unsafe_cast_of_account_data_into_vaul\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ load_foreign_config };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-001 - Unsafe cast of account data into `VaultConfig` in load_foreign_config()\n#[tokio::test]\nasync fn sg_sol_001_unsafe_cast_of_account_data_into_vaul() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Create an account with the program's own PDA-looking address (or any short account) and pass it in.\n    // Step 2 - The cast reads whatever bytes exist - attacker-chosen numbers become `limiter`, authority, etc.\n    // Step 3 - Use the forged field to unlock the privileged branch of the handler.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-001: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_001_unsafe_cast_of_account_data_into_vaul_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Use Anchor's typed account (`Account<'info, VaultConfig>`), which verifies owner + discriminator.\n    //   If raw access is unavoidable: check `account.owner == &id()`, then `VaultConfig::try_deserialize(&mut &data[8..])` and never dereference a cast.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority",
        "`remaining_accounts` are length-checked **before** indexing and validated individually -"
      ],
      "cwe": "CWE-125",
      "tags": [
        "solana",
        "unsafe",
        "deserialization",
        "DESER-001"
      ],
      "corroborated_by": [
        "ACCT-001 (SG-SOL-014)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-EVM-002",
      "detector": "evm_sig_replay",
      "rule": "SR-002",
      "title": "No spend-once tracking: claimTreasuryGrant() can be replayed forever",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.9,
      "score": 8.64,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 87,
        "end_line": 92,
        "function": "claimTreasuryGrant",
        "contract": "HyperVault"
      },
      "description": "claimTreasuryGrant() pays out on a verified signature but no nonce, no mapping write and no already-used check exists anywhere in the contract. The signer's single authorisation is therefore an unlimited mint: the same (v, r, s) can be submitted in an unbounded number of transactions by anyone who has seen it once - including from public mempool history.",
      "evidence": "function claimTreasuryGrant(bytes32 doc, uint8 v, bytes32 r, bytes32 s, uint256 amount) external {",
      "exploit_sketch": [
        "Observe one legitimate use of claimTreasuryGrant() (mempool, front-end, an airdrop claim).",
        "Call it again with the identical doc/v/r/s - the require() on the recovered address still passes.",
        "Repeat until the contract is empty; gas is the only cost."
      ],
      "patch_sketch": [
        "Track spend: `require(!used[hash]); used[hash] = true;` keyed on the full digest.",
        "Or carry a per-signer monotonic nonce inside the signed payload and compare it to storage."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-002_sg_evm_002_no_spend_once_trackin.t.sol",
        "how_to_run": "forge test --match-contract SgEvm002NoSpendOnceTrackingClaimtreasuryHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-002 - No spend-once tracking: claimTreasuryGrant() can be replayed forever\n// Severity: critical (confidence 0.90)\n// Rule    : SR-002\n// Skill   : skills/signature-replay/SKILL.md\n// Location: solidity/HyperVault.sol:87 (claimTreasuryGrant)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm002NoSpendOnceTrackingClaimtreasuryHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm002NoSpendOnceTrackingClaimtreasuryHarness\n/// @notice PoC harness for SG-EVM-002.\ncontract SgEvm002NoSpendOnceTrackingClaimtreasuryHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice No spend-once tracking: claimTreasuryGrant() can be replayed forever\n    function test_sg_evm_002_no_spend_once_tracking_claimtreasuryg() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Observe one legitimate use of claimTreasuryGrant() (mempool, front-end, an airdrop claim).\n        // Step 2 - Call it again with the identical doc/v/r/s - the require() on the recovered address still passes.\n        // Step 3 - Repeat until the contract is empty; gas is the only cost.\n\n        vm.prank(attacker);\n        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_002_no_spend_once_tracking_claimtreasuryg_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Track spend: `require(!used[hash]); used[hash] = true;` keyed on the full digest.\n        //   Or carry a per-signer monotonic nonce inside the signed payload and compare it to storage.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/signature-replay/SKILL.md",
      "skill_checklist": [
        "A spend-once record exists (per-hash mapping or per-signer nonce) and is written in the same",
        "Batch payloads bind the whole payload hash (no partial-subset replay)."
      ],
      "cwe": "CWE-294",
      "tags": [
        "signature",
        "replay",
        "missing-nonce",
        "SR-002"
      ],
      "corroborated_by": [
        "CEI-001 (SG-EVM-003)",
        "EC-001 (SG-EVM-010)",
        "EC-003 (SG-EVM-024)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-003",
      "detector": "evm_reentrancy",
      "rule": "CEI-001",
      "title": "Reentrancy: state updated after external call in claimTreasuryGrant()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.9,
      "score": 8.64,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 90,
        "end_line": 91,
        "function": "claimTreasuryGrant",
        "contract": "HyperVault"
      },
      "description": "claimTreasuryGrant() performs an external interaction (payable(msg.sender).transfer) at line 90 and only updates contract state afterwards (borrowed). A callee can re-enter claimTreasuryGrant() during that callback and still see the pre-call balance, so the same funds can be withdrawn repeatedly. No reentrancy guard and no checks-effects-interactions ordering were found in this function.",
      "evidence": "payable(msg.sender).transfer(amount);",
      "exploit_sketch": [
        "Deploy a malicious callee whose fallback/receive() calls back into claimTreasuryGrant().",
        "On the first call, pass an amount the contract still thinks you are owed.",
        "Inside the callback, call claimTreasuryGrant() again before the balance subtraction at line 91 has executed - the old balance is still readable.",
        "Repeat until the contract's native/ERC20 balance is drained."
      ],
      "patch_sketch": [
        "Update all accounting before the external call (checks-effects-interactions).",
        "Or add OpenZeppelin ReentrancyGuard: `nonReentrant` on claimTreasuryGrant().",
        "Prefer push payments with an explicit withdraw-then-send ordering."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-003_sg_evm_003_reentrancy_state_upda.t.sol",
        "how_to_run": "forge test --match-contract SgEvm003ReentrancyStateUpdatedAfterExterHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-003 - Reentrancy: state updated after external call in claimTreasuryGrant()\n// Severity: critical (confidence 0.90)\n// Rule    : CEI-001\n// Skill   : skills/reentrancy/SKILL.md\n// Location: solidity/HyperVault.sol:90 (claimTreasuryGrant)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm003ReentrancyStateUpdatedAfterExterHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm003ReentrancyStateUpdatedAfterExterHarness\n/// @notice PoC harness for SG-EVM-003.\ncontract SgEvm003ReentrancyStateUpdatedAfterExterHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Reentrancy: state updated after external call in claimTreasuryGrant()\n    function test_sg_evm_003_reentrancy_state_updated_after_extern() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Deploy a malicious callee whose fallback/receive() calls back into claimTreasuryGrant().\n        // Step 2 - On the first call, pass an amount the contract still thinks you are owed.\n        // Step 3 - Inside the callback, call claimTreasuryGrant() again before the balance subtraction at line 91 has executed - the old balance is still readable.\n\n        vm.prank(attacker);\n        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_003_reentrancy_state_updated_after_extern_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Update all accounting before the external call (checks-effects-interactions).\n        //   Or add OpenZeppelin ReentrancyGuard: `nonReentrant` on claimTreasuryGrant().\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/reentrancy/SKILL.md",
      "skill_checklist": [
        "State writes (`balances[...]`, totals, flags, allowances, nonces) are ordered strictly",
        "A reentrancy guard (`nonReentrant`, OZ `ReentrancyGuard`, ERC-7201 transient storage",
        "before** the interaction - checks-effects-interactions holds.",
        "Callbacks from the callee (`onERC721Received`, `tokensReceived`, `ERC4626` deposit hooks,"
      ],
      "cwe": "CWE-841",
      "tags": [
        "reentrancy",
        "eth",
        "value-transfer",
        "CEI-001"
      ],
      "corroborated_by": [
        "EC-001 (SG-EVM-010)",
        "EC-003 (SG-EVM-024)",
        "SR-001 (SG-EVM-017)",
        "SR-002 (SG-EVM-002)",
        "SR-003 (SG-EVM-020)",
        "SR-004 (SG-EVM-023)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-004",
      "detector": "evm_selfdestruct",
      "rule": "SD-001",
      "title": "Reachable `selfdestruct` with caller-chosen beneficiary in decommission()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.9,
      "score": 8.64,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 102,
        "end_line": 102,
        "function": "decommission",
        "contract": "HyperVault"
      },
      "description": "decommission() can destroy IHyperCorePriceOracle and force-send the whole native balance to an address the caller picks (`beneficiary`). There is no authorisation check at all on this path. Destructible state also kills any index that assumed code persists, and it makes an irreversible action available without a timelock or governance delay.",
      "evidence": "selfdestruct(beneficiary);",
      "exploit_sketch": [
        "Call decommission() (directly, or through the unguarded entry that reaches it).",
        "The contract's storage is wiped and its ETH balance is pushed to the chosen beneficiary.",
        "Every user balance recorded in the contract becomes unrecoverable."
      ],
      "patch_sketch": [
        "Remove selfdestruct from application code; use a pause flag plus an explicit withdrawal route.",
        "If a migration really is needed, gate it behind a timelock + multi-sig governance role and a fixed beneficiary.",
        "Note that EIP-678 (Cancun and later) limits selfdestruct to same-transaction creation, but the storage/balance sweep still applies on pre-Pectra deployments and on HyperEVM."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-004_sg_evm_004_reachable_selfdestruc.t.sol",
        "how_to_run": "forge test --match-contract SgEvm004ReachableSelfdestructWithCallerCHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-004 - Reachable `selfdestruct` with caller-chosen beneficiary in decommission()\n// Severity: critical (confidence 0.90)\n// Rule    : SD-001\n// Skill   : skills/fund-recovery/SKILL.md\n// Location: solidity/HyperVault.sol:102 (decommission)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm004ReachableSelfdestructWithCallerCHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm004ReachableSelfdestructWithCallerCHarness\n/// @notice PoC harness for SG-EVM-004.\ncontract SgEvm004ReachableSelfdestructWithCallerCHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Reachable `selfdestruct` with caller-chosen beneficiary in decommission()\n    function test_sg_evm_004_reachable_selfdestruct_with_caller_ch() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Call decommission() (directly, or through the unguarded entry that reaches it).\n        // Step 2 - The contract's storage is wiped and its ETH balance is pushed to the chosen beneficiary.\n        // Step 3 - Every user balance recorded in the contract becomes unrecoverable.\n\n        vm.prank(attacker);\n        // TODO: call HyperVault.decommission(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_004_reachable_selfdestruct_with_caller_ch_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Remove selfdestruct from application code; use a pause flag plus an explicit withdrawal route.\n        //   If a migration really is needed, gate it behind a timelock + multi-sig governance role and a fixed beneficiary.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/fund-recovery/SKILL.md",
      "skill_checklist": [
        "`selfdestruct` does not exist in application code. If it does, it is reachable only behind a"
      ],
      "cwe": "CWE-284",
      "tags": [
        "selfdestruct",
        "irreversible",
        "SD-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-005",
      "detector": "evm_reentrancy",
      "rule": "CEI-001",
      "title": "Reentrancy: state updated after external call in withdraw()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.9,
      "score": 8.64,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 74,
        "end_line": 77,
        "function": "withdraw",
        "contract": "Vault"
      },
      "description": "withdraw() performs an external interaction (msg.sender.call) at line 74 and only updates contract state afterwards (balanceOf[msg.sender], totalDeposits). A callee can re-enter withdraw() during that callback and still see the pre-call balance, so the same funds can be withdrawn repeatedly. No reentrancy guard and no checks-effects-interactions ordering were found in this function.",
      "evidence": "(bool ok, ) = msg.sender.call{value: amount}(\"\");",
      "exploit_sketch": [
        "Deploy a malicious callee whose fallback/receive() calls back into withdraw().",
        "On the first call, pass an amount the contract still thinks you are owed.",
        "Inside the callback, call withdraw() again before the balance subtraction at line 76 has executed - the old balance is still readable.",
        "Repeat until the contract's native/ERC20 balance is drained."
      ],
      "patch_sketch": [
        "Update all accounting before the external call (checks-effects-interactions).",
        "Or add OpenZeppelin ReentrancyGuard: `nonReentrant` on withdraw().",
        "Prefer push payments with an explicit withdraw-then-send ordering."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-005_sg_evm_005_reentrancy_state_upda.t.sol",
        "how_to_run": "forge test --match-contract SgEvm005ReentrancyStateUpdatedAfterExterHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-005 - Reentrancy: state updated after external call in withdraw()\n// Severity: critical (confidence 0.90)\n// Rule    : CEI-001\n// Skill   : skills/reentrancy/SKILL.md\n// Location: solidity/Vault.sol:74 (withdraw)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm005ReentrancyStateUpdatedAfterExterHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm005ReentrancyStateUpdatedAfterExterHarness\n/// @notice PoC harness for SG-EVM-005.\ncontract SgEvm005ReentrancyStateUpdatedAfterExterHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Reentrancy: state updated after external call in withdraw()\n    function test_sg_evm_005_reentrancy_state_updated_after_extern() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Deploy a malicious callee whose fallback/receive() calls back into withdraw().\n        // Step 2 - On the first call, pass an amount the contract still thinks you are owed.\n        // Step 3 - Inside the callback, call withdraw() again before the balance subtraction at line 76 has executed - the old balance is still readable.\n\n        vm.prank(attacker);\n        // TODO: call Vault.withdraw(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_005_reentrancy_state_updated_after_extern_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Update all accounting before the external call (checks-effects-interactions).\n        //   Or add OpenZeppelin ReentrancyGuard: `nonReentrant` on withdraw().\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/reentrancy/SKILL.md",
      "skill_checklist": [
        "State writes (`balances[...]`, totals, flags, allowances, nonces) are ordered strictly",
        "A reentrancy guard (`nonReentrant`, OZ `ReentrancyGuard`, ERC-7201 transient storage",
        "before** the interaction - checks-effects-interactions holds.",
        "Callbacks from the callee (`onERC721Received`, `tokensReceived`, `ERC4626` deposit hooks,"
      ],
      "cwe": "CWE-841",
      "tags": [
        "reentrancy",
        "eth",
        "value-transfer",
        "CEI-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-006",
      "detector": "evm_access",
      "rule": "AC-001",
      "title": "Missing access control on adminDrain()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.9,
      "score": 8.64,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 83,
        "end_line": 88,
        "function": "adminDrain",
        "contract": "Vault"
      },
      "description": "adminDrain() is external, moves value to `to`, and writes payout, yet nothing in the function proves who the caller is: no `only*` modifier, no `require(msg.sender == ...)`, no role check, and no caller-scoped accounting. So the state it touches is not limited to the caller's own balance - any address can call it. This is a total-loss path: the destination is fully caller-chosen.",
      "evidence": "function adminDrain(address to, uint256 amount) external {",
      "exploit_sketch": [
        "Call adminDrain() from an EOA you control, with the arguments you want.",
        "No key compromise or front-running is required - the entry point is permissionless.",
        "Repeat until the shared balance is gone (or until every victim's claim has been consumed)."
      ],
      "patch_sketch": [
        "Add `onlyOwner` / AccessControl `onlyRole(GUARDIAN_ROLE)` to adminDrain().",
        "Or make the function self-service: derive the destination from `msg.sender` and let users pull their own funds.",
        "Emit an event and add a per-call cap so a wrong grant of authority is bounded."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-006_sg_evm_006_missing_access_contro.t.sol",
        "how_to_run": "forge test --match-contract SgEvm006MissingAccessControlOnAdmindrainHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-006 - Missing access control on adminDrain()\n// Severity: critical (confidence 0.90)\n// Rule    : AC-001\n// Skill   : skills/access-control/SKILL.md\n// Location: solidity/Vault.sol:83 (adminDrain)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm006MissingAccessControlOnAdmindrainHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm006MissingAccessControlOnAdmindrainHarness\n/// @notice PoC harness for SG-EVM-006.\ncontract SgEvm006MissingAccessControlOnAdmindrainHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Missing access control on adminDrain()\n    function test_sg_evm_006_missing_access_control_on_admindrain() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Call adminDrain() from an EOA you control, with the arguments you want.\n        // Step 2 - No key compromise or front-running is required - the entry point is permissionless.\n        // Step 3 - Repeat until the shared balance is gone (or until every victim's claim has been consumed).\n\n        vm.prank(attacker);\n        // TODO: call Vault.adminDrain(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_006_missing_access_control_on_admindrain_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Add `onlyOwner` / AccessControl `onlyRole(GUARDIAN_ROLE)` to adminDrain().\n        //   Or make the function self-service: derive the destination from `msg.sender` and let users pull their own funds.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/access-control/SKILL.md",
      "skill_checklist": [
        "Each privileged function has either a modifier (`onlyOwner`, `onlyRole`, `onlyAdmin`)",
        "Build the list of externally callable functions and mark which ones mutate privileged state."
      ],
      "cwe": "CWE-284",
      "tags": [
        "access-control",
        "value-flow",
        "AC-001"
      ],
      "corroborated_by": [
        "CEI-001 (SG-EVM-007)",
        "SD-002 (SG-EVM-009)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-007",
      "detector": "evm_reentrancy",
      "rule": "CEI-001",
      "title": "Reentrancy: state updated after external call in adminDrain()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.9,
      "score": 8.64,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 85,
        "end_line": 87,
        "function": "adminDrain",
        "contract": "Vault"
      },
      "description": "adminDrain() performs an external interaction (to.call) at line 85 and only updates contract state afterwards (totalDeposits). A callee can re-enter adminDrain() during that callback and still see the pre-call balance, so the same funds can be withdrawn repeatedly. No reentrancy guard and no checks-effects-interactions ordering were found in this function.",
      "evidence": "(bool sent, ) = to.call{value: payout}(\"\");",
      "exploit_sketch": [
        "Deploy a malicious callee whose fallback/receive() calls back into adminDrain().",
        "On the first call, pass an amount the contract still thinks you are owed.",
        "Inside the callback, call adminDrain() again before the balance subtraction at line 87 has executed - the old balance is still readable.",
        "Repeat until the contract's native/ERC20 balance is drained."
      ],
      "patch_sketch": [
        "Update all accounting before the external call (checks-effects-interactions).",
        "Or add OpenZeppelin ReentrancyGuard: `nonReentrant` on adminDrain().",
        "Prefer push payments with an explicit withdraw-then-send ordering."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-007_sg_evm_007_reentrancy_state_upda.t.sol",
        "how_to_run": "forge test --match-contract SgEvm007ReentrancyStateUpdatedAfterExterHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-007 - Reentrancy: state updated after external call in adminDrain()\n// Severity: critical (confidence 0.90)\n// Rule    : CEI-001\n// Skill   : skills/reentrancy/SKILL.md\n// Location: solidity/Vault.sol:85 (adminDrain)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm007ReentrancyStateUpdatedAfterExterHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm007ReentrancyStateUpdatedAfterExterHarness\n/// @notice PoC harness for SG-EVM-007.\ncontract SgEvm007ReentrancyStateUpdatedAfterExterHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Reentrancy: state updated after external call in adminDrain()\n    function test_sg_evm_007_reentrancy_state_updated_after_extern() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Deploy a malicious callee whose fallback/receive() calls back into adminDrain().\n        // Step 2 - On the first call, pass an amount the contract still thinks you are owed.\n        // Step 3 - Inside the callback, call adminDrain() again before the balance subtraction at line 87 has executed - the old balance is still readable.\n\n        vm.prank(attacker);\n        // TODO: call Vault.adminDrain(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_007_reentrancy_state_updated_after_extern_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Update all accounting before the external call (checks-effects-interactions).\n        //   Or add OpenZeppelin ReentrancyGuard: `nonReentrant` on adminDrain().\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/reentrancy/SKILL.md",
      "skill_checklist": [
        "State writes (`balances[...]`, totals, flags, allowances, nonces) are ordered strictly",
        "A reentrancy guard (`nonReentrant`, OZ `ReentrancyGuard`, ERC-7201 transient storage",
        "before** the interaction - checks-effects-interactions holds.",
        "Callbacks from the callee (`onERC721Received`, `tokensReceived`, `ERC4626` deposit hooks,"
      ],
      "cwe": "CWE-841",
      "tags": [
        "reentrancy",
        "eth",
        "value-transfer",
        "CEI-001"
      ],
      "corroborated_by": [
        "AC-001 (SG-EVM-006)",
        "SD-002 (SG-EVM-009)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-SOL-002",
      "detector": "solana_signer",
      "rule": "SIGNER-001",
      "title": "Missing signer check on `raw_config` in load_foreign_config()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.88,
      "score": 8.568,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 217,
        "end_line": 217,
        "function": "load_foreign_config",
        "contract": "lib.rs"
      },
      "description": "`raw_config` is declared as AccountInfo<info> in the `LoadConfig` accounts struct, so Anchor cannot prove it was signed. load_foreign_config() still uses it to authorise a privileged state change. On Solana an unsigned account is just bytes anyone can craft: an attacker supplies their own account with the right key/shape and the program accepts the instruction. The handler contains no `is_signer` check either and no `Signer` account exists in the struct at all.",
      "evidence": "pub raw_config: AccountInfo<'info>,",
      "exploit_sketch": [
        "Build the instruction with a self-created account in the `raw_config` slot (it does not need the real owner's key pair). ",
        "Send the transaction - the program sees a well-formed account and no signature requirement fails.",
        "Repeat for every victim's data the handler writes through that account."
      ],
      "patch_sketch": [
        "Type it as `Signer<'info>`: `raw_config: Signer<'info>` (Anchor enforces the signature).",
        "Or add an explicit `require!(ctx.accounts.raw_config.is_signer, VaultError::MissingSigner);`.",
        "Better: bind it to state with `has_one = raw_config` plus a PDA `seeds` constraint."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-002_sg_sol_002_missing_signer_check_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_002_missing_signer_check_on_raw_config_in",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-002 - Missing signer check on `raw_config` in load_foreign_config()\n// Severity: critical (confidence 0.88)\n// Rule    : SIGNER-001\n// Skill   : skills/solana-signer/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:217 (load_foreign_config)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_002_missing_signer_check_on_raw_config_in\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ load_foreign_config };\n// use vault::{Accounts, lib.rs};\n\n/// PoC harness for SG-SOL-002 - Missing signer check on `raw_config` in load_foreign_config()\n#[tokio::test]\nasync fn sg_sol_002_missing_signer_check_on_raw_config_in() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Build the instruction with a self-created account in the `raw_config` slot (it does not need the real owner's key pair). \n    // Step 2 - Send the transaction - the program sees a well-formed account and no signature requirement fails.\n    // Step 3 - Repeat for every victim's data the handler writes through that account.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-002: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_002_missing_signer_check_on_raw_config_in_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Type it as `Signer<'info>`: `raw_config: Signer<'info>` (Anchor enforces the signature).\n    //   Or add an explicit `require!(ctx.accounts.raw_config.is_signer, VaultError::MissingSigner);`.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-signer/SKILL.md",
      "skill_checklist": [
        "Every account whose **signature** is required is typed `Signer<'info>`; the type system is",
        "The key stored as the future authority (`vault.authority = authority.key()`) comes from a"
      ],
      "cwe": "CWE-287",
      "tags": [
        "solana",
        "missing-signer",
        "SIGNER-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-008",
      "detector": "evm_oracle",
      "rule": "OR-001",
      "title": "AMM spot price used as valuation in assetValue()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.88,
      "score": 8.568,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 120,
        "end_line": 120,
        "function": "assetValue",
        "contract": "Vault"
      },
      "description": "assetValue() derives a price from in-pool reserves (`getReserves`), which is the pool's own balance ratio rather than a traded price. Any caller with enough capital - typically a flash loan - can skew that ratio in a single transaction, and the contract will read the manipulated number in the same block. There is no time-weighting and no second source here.",
      "evidence": "(uint112 reserve0, uint112 reserve1, ) = pool.getReserves();",
      "exploit_sketch": [
        "Flash-loan a large amount of one side of the pool.",
        "Swap it into the pool to move reserves (and therefore the printed price) in the same tx.",
        "Call assetValue() - the inflated price is now the contract's truth - then mint/borrow/liquidate.",
        "Swap back and repay the loan; the profit is the difference in the contract's accounting."
      ],
      "patch_sketch": [
        "Use a time-weighted price (Chainlink TWAP or a multi-block Uniswap V2 `cumulativePrice` window).",
        "Never let an in-block reserve ratio set collateral or payout values.",
        "Add a max-deviation guard against a second source and reject out-of-band prints."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-008_sg_evm_008_amm_spot_price_used_a.t.sol",
        "how_to_run": "forge test --match-contract SgEvm008AmmSpotPriceUsedAsValuationInAssHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-008 - AMM spot price used as valuation in assetValue()\n// Severity: critical (confidence 0.88)\n// Rule    : OR-001\n// Skill   : skills/oracle-price/SKILL.md\n// Location: solidity/Vault.sol:120 (assetValue)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm008AmmSpotPriceUsedAsValuationInAssHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm008AmmSpotPriceUsedAsValuationInAssHarness\n/// @notice PoC harness for SG-EVM-008.\ncontract SgEvm008AmmSpotPriceUsedAsValuationInAssHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice AMM spot price used as valuation in assetValue()\n    function test_sg_evm_008_amm_spot_price_used_as_valuation_in_a() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Flash-loan a large amount of one side of the pool.\n        // Step 2 - Swap it into the pool to move reserves (and therefore the printed price) in the same tx.\n        // Step 3 - Call assetValue() - the inflated price is now the contract's truth - then mint/borrow/liquidate.\n\n        vm.prank(attacker);\n        // TODO: call Vault.assetValue(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_008_amm_spot_price_used_as_valuation_in_a_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Use a time-weighted price (Chainlink TWAP or a multi-block Uniswap V2 `cumulativePrice` window).\n        //   Never let an in-block reserve ratio set collateral or payout values.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/oracle-price/SKILL.md",
      "skill_checklist": [
        "Identify the pricing source: in-pool reserves (spot), a single Chainlink round, a",
        "The price cannot be moved inside the same transaction that consumes it (flash-loan",
        "A second, independent source or a max-deviation circuit breaker exists for anything that"
      ],
      "cwe": "CWE-1231",
      "tags": [
        "oracle",
        "price-manipulation",
        "flash-loan",
        "OR-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-003",
      "detector": "solana_signer",
      "rule": "SIGNER-002",
      "title": "withdraw() writes state / moves value with no `Signer` account at all",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.85,
      "score": 8.46,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 54,
        "end_line": 54,
        "function": "withdraw",
        "contract": ""
      },
      "description": "The `Withdraw` constraint list contains no `Signer`, yet withdraw() performs a privileged operation (`.lamports_mut(`). Permissionless mutation is exactly the class of bug that empties programs on Solana: there is no `msg.sender` equivalent to fall back on, so the account list is the only authorisation surface.",
      "evidence": "ctx.accounts.user_authority.lamports_mut(|l| *l += amount)?;",
      "exploit_sketch": [
        "Call withdraw directly from any wallet - no signature from the configured authority is required.",
        "Point the writable accounts at whatever you want changed."
      ],
      "patch_sketch": [
        "Add `authority: Signer<'info>` and `#[account(constraint = authority.key() == vault_config.authority)]`.",
        "Or derive the writable account as a PDA and sign with `seeds` so only the program can author it."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-003_sg_sol_003_withdraw_writes_state.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_003_withdraw_writes_state_moves_value_wit",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-003 - withdraw() writes state / moves value with no `Signer` account at all\n// Severity: critical (confidence 0.85)\n// Rule    : SIGNER-002\n// Skill   : skills/solana-signer/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:54 (withdraw)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_003_withdraw_writes_state_moves_value_wit\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ withdraw };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-003 - withdraw() writes state / moves value with no `Signer` account at all\n#[tokio::test]\nasync fn sg_sol_003_withdraw_writes_state_moves_value_wit() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Call withdraw directly from any wallet - no signature from the configured authority is required.\n    // Step 2 - Point the writable accounts at whatever you want changed.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-003: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_003_withdraw_writes_state_moves_value_wit_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Add `authority: Signer<'info>` and `#[account(constraint = authority.key() == vault_config.authority)]`.\n    //   Or derive the writable account as a PDA and sign with `seeds` so only the program can author it.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-signer/SKILL.md",
      "skill_checklist": [
        "Every account whose **signature** is required is typed `Signer<'info>`; the type system is"
      ],
      "cwe": "CWE-287",
      "tags": [
        "solana",
        "missing-signer",
        "privilege",
        "SIGNER-002"
      ],
      "corroborated_by": [
        "AUTH-003 (SG-SOL-021)",
        "MATH-002 (SG-SOL-023)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-SOL-004",
      "detector": "solana_signer",
      "rule": "SIGNER-002",
      "title": "slash() writes state / moves value with no `Signer` account at all",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.85,
      "score": 8.46,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 68,
        "end_line": 68,
        "function": "slash",
        "contract": ""
      },
      "description": "The `Slash` constraint list contains no `Signer`, yet slash() performs a privileged operation (`+=`). Permissionless mutation is exactly the class of bug that empties programs on Solana: there is no `msg.sender` equivalent to fall back on, so the account list is the only authorisation surface.",
      "evidence": "ctx.accounts.vault_token_account.amount += shares;",
      "exploit_sketch": [
        "Call slash directly from any wallet - no signature from the configured authority is required.",
        "Point the writable accounts at whatever you want changed."
      ],
      "patch_sketch": [
        "Add `authority: Signer<'info>` and `#[account(constraint = authority.key() == vault_config.authority)]`.",
        "Or derive the writable account as a PDA and sign with `seeds` so only the program can author it."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-004_sg_sol_004_slash_writes_state_mo.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_004_slash_writes_state_moves_value_with_n",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-004 - slash() writes state / moves value with no `Signer` account at all\n// Severity: critical (confidence 0.85)\n// Rule    : SIGNER-002\n// Skill   : skills/solana-signer/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:68 (slash)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_004_slash_writes_state_moves_value_with_n\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ slash };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-004 - slash() writes state / moves value with no `Signer` account at all\n#[tokio::test]\nasync fn sg_sol_004_slash_writes_state_moves_value_with_n() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Call slash directly from any wallet - no signature from the configured authority is required.\n    // Step 2 - Point the writable accounts at whatever you want changed.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-004: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_004_slash_writes_state_moves_value_with_n_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Add `authority: Signer<'info>` and `#[account(constraint = authority.key() == vault_config.authority)]`.\n    //   Or derive the writable account as a PDA and sign with `seeds` so only the program can author it.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-signer/SKILL.md",
      "skill_checklist": [
        "Every account whose **signature** is required is typed `Signer<'info>`; the type system is"
      ],
      "cwe": "CWE-287",
      "tags": [
        "solana",
        "missing-signer",
        "privilege",
        "SIGNER-002"
      ],
      "corroborated_by": [
        "CPI-003 (SG-SOL-015)",
        "MATH-004 (SG-SOL-025)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-SOL-005",
      "detector": "solana_signer",
      "rule": "SIGNER-002",
      "title": "set_limiter() writes state / moves value with no `Signer` account at all",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.85,
      "score": 8.46,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 76,
        "end_line": 76,
        "function": "set_limiter",
        "contract": ""
      },
      "description": "The `SetLimiter` constraint list contains no `Signer`, yet set_limiter() performs a privileged operation (`ctx.accounts.vault_config.limiter = `). Permissionless mutation is exactly the class of bug that empties programs on Solana: there is no `msg.sender` equivalent to fall back on, so the account list is the only authorisation surface.",
      "evidence": "ctx.accounts.vault_config.limiter = limiter;",
      "exploit_sketch": [
        "Call set_limiter directly from any wallet - no signature from the configured authority is required.",
        "Point the writable accounts at whatever you want changed."
      ],
      "patch_sketch": [
        "Add `authority: Signer<'info>` and `#[account(constraint = authority.key() == vault_config.authority)]`.",
        "Or derive the writable account as a PDA and sign with `seeds` so only the program can author it."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-005_sg_sol_005_set_limiter_writes_st.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_005_set_limiter_writes_state_moves_value_",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-005 - set_limiter() writes state / moves value with no `Signer` account at all\n// Severity: critical (confidence 0.85)\n// Rule    : SIGNER-002\n// Skill   : skills/solana-signer/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:76 (set_limiter)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_005_set_limiter_writes_state_moves_value_\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ set_limiter };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-005 - set_limiter() writes state / moves value with no `Signer` account at all\n#[tokio::test]\nasync fn sg_sol_005_set_limiter_writes_state_moves_value_() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Call set_limiter directly from any wallet - no signature from the configured authority is required.\n    // Step 2 - Point the writable accounts at whatever you want changed.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-005: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_005_set_limiter_writes_state_moves_value__after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Add `authority: Signer<'info>` and `#[account(constraint = authority.key() == vault_config.authority)]`.\n    //   Or derive the writable account as a PDA and sign with `seeds` so only the program can author it.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-signer/SKILL.md",
      "skill_checklist": [
        "Every account whose **signature** is required is typed `Signer<'info>`; the type system is"
      ],
      "cwe": "CWE-287",
      "tags": [
        "solana",
        "missing-signer",
        "privilege",
        "SIGNER-002"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-006",
      "detector": "solana_accounts",
      "rule": "ACCT-002",
      "title": "Token CPI over unvalidated account `user_token_account` in deposit()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.8,
      "score": 8.28,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 39,
        "end_line": 39,
        "function": "deposit",
        "contract": ""
      },
      "description": "deposit() hands `ctx.accounts.user_token_account` to an SPL token CPI while the account is only an UncheckedAccount. Nothing proves its owner is the token program, that it is the vault's token account, or that its mint is the expected one. The CPI happily operates on whatever account the client supplied, which is the standard way vaults end up paying out of the wrong pot.",
      "evidence": "from: ctx.accounts.user_token_account.to_account_info(),",
      "exploit_sketch": [
        "Point `user_token_account` at a token account you control (or at any account whose data you can shape).",
        "The vault debits/credits the wrong account; your own balance is the source of truth for it."
      ],
      "patch_sketch": [
        "`#[account(constraint = user_token_account.owner == program_authority.key() && user_token_account.mint == expected_mint)]`",
        "Type it `Account<'info, TokenAccount>` so Anchor deserialises and checks the owner."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-006_sg_sol_006_token_cpi_over_unvali.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_006_token_cpi_over_unvalidated_account_us",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-006 - Token CPI over unvalidated account `user_token_account` in deposit()\n// Severity: critical (confidence 0.80)\n// Rule    : ACCT-002\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:39 (deposit)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_006_token_cpi_over_unvalidated_account_us\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ deposit };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-006 - Token CPI over unvalidated account `user_token_account` in deposit()\n#[tokio::test]\nasync fn sg_sol_006_token_cpi_over_unvalidated_account_us() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Point `user_token_account` at a token account you control (or at any account whose data you can shape).\n    // Step 2 - The vault debits/credits the wrong account; your own balance is the source of truth for it.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-006: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_006_token_cpi_over_unvalidated_account_us_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   `#[account(constraint = user_token_account.owner == program_authority.key() && user_token_account.mint == expected_mint)]`\n    //   Type it `Account<'info, TokenAccount>` so Anchor deserialises and checks the owner.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority"
      ],
      "cwe": "CWE-863",
      "tags": [
        "solana",
        "cpi",
        "account-validation",
        "ACCT-002"
      ],
      "corroborated_by": [
        "MATH-001 (SG-SOL-010)",
        "MATH-002 (SG-SOL-022)",
        "MATH-003 (SG-SOL-016)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-007",
      "detector": "solana_cpi",
      "rule": "CPI-002",
      "title": "CPI target `signer_program` is an unvalidated account, not a Program",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.8,
      "score": 8.28,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 208,
        "end_line": 208,
        "function": "settle_external",
        "contract": ""
      },
      "description": "`signer_program` is declared as UncheckedAccount in `SettleExternal` and is used as the program id for a cross-program call. Anchor only checks that an account is a *program* when it is typed `Program<'info, T>` (or when `is_program()` is called). Here the client picks the 'program', so the instruction is executed against code the program never vouched for - typically a spoofed token program that returns success without moving anything.",
      "evidence": "pub signer_program: UncheckedAccount<'info>,",
      "exploit_sketch": [
        "Deploy a fake program whose address you pass as `signer_program`.",
        "It deserialises the args, returns Ok, and moves nothing.",
        "The vault credits your position anyway - mint-without-deposit."
      ],
      "patch_sketch": [
        "Type the account `Program<'info, anchor_spl::token::Token>` (or `System`).",
        "Or `require!(ctx.accounts.signer_program.is_program(), VaultError::InvalidProgram);`.",
        "Even better, use the CPI helper (`token::transfer`) which resolves the program from `CpiContext` types instead of a client field."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-007_sg_sol_007_cpi_target_signer_pro.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_007_cpi_target_signer_program_is_an_unval",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-007 - CPI target `signer_program` is an unvalidated account, not a Program\n// Severity: critical (confidence 0.80)\n// Rule    : CPI-002\n// Skill   : skills/solana-cpi/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:208 (settle_external)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_007_cpi_target_signer_program_is_an_unval\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ settle_external };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-007 - CPI target `signer_program` is an unvalidated account, not a Program\n#[tokio::test]\nasync fn sg_sol_007_cpi_target_signer_program_is_an_unval() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Deploy a fake program whose address you pass as `signer_program`.\n    // Step 2 - It deserialises the args, returns Ok, and moves nothing.\n    // Step 3 - The vault credits your position anyway - mint-without-deposit.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-007: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_007_cpi_target_signer_program_is_an_unval_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Type the account `Program<'info, anchor_spl::token::Token>` (or `System`).\n    //   Or `require!(ctx.accounts.signer_program.is_program(), VaultError::InvalidProgram);`.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-cpi/SKILL.md",
      "skill_checklist": [
        "The callee is a `Program<'info, T>` account (or `is_program()` verified), never a raw"
      ],
      "cwe": "CWE-252",
      "tags": [
        "solana",
        "cpi",
        "program-validation",
        "CPI-002"
      ],
      "corroborated_by": [
        "ACCT-001 (SG-SOL-013)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-EVM-009",
      "detector": "evm_selfdestruct",
      "rule": "SD-002",
      "title": "Value sent to a caller-chosen address in adminDrain()",
      "severity": "critical",
      "severity_weight": 9.0,
      "confidence": 0.8,
      "score": 8.28,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 85,
        "end_line": 85,
        "function": "adminDrain",
        "contract": "Vault"
      },
      "description": "adminDrain() moves native value to `to`, which is supplied by the caller rather than derived from the contract's own accounting. It is unguarded. Any caller (or any caller that can reach this path) chooses the destination, so shared funds can be redirected at will.",
      "evidence": "(bool sent, ) = to.call{value: payout}(\"\");",
      "exploit_sketch": [
        "Call adminDrain() with `to` set to an address you control.",
        "Repeat for each victim's recorded balance if the function is parameterised by user."
      ],
      "patch_sketch": [
        "Derive the recipient from the caller (`msg.sender`) or from a per-user withdrawal map.",
        "Use pull-payments: credit an internal balance, let users claim it themselves."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-009_sg_evm_009_value_sent_to_a_calle.t.sol",
        "how_to_run": "forge test --match-contract SgEvm009ValueSentToACallerChosenAddressIHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-009 - Value sent to a caller-chosen address in adminDrain()\n// Severity: critical (confidence 0.80)\n// Rule    : SD-002\n// Skill   : skills/fund-recovery/SKILL.md\n// Location: solidity/Vault.sol:85 (adminDrain)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm009ValueSentToACallerChosenAddressIHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm009ValueSentToACallerChosenAddressIHarness\n/// @notice PoC harness for SG-EVM-009.\ncontract SgEvm009ValueSentToACallerChosenAddressIHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Value sent to a caller-chosen address in adminDrain()\n    function test_sg_evm_009_value_sent_to_a_caller_chosen_address() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Call adminDrain() with `to` set to an address you control.\n        // Step 2 - Repeat for each victim's recorded balance if the function is parameterised by user.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call Vault.adminDrain(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_009_value_sent_to_a_caller_chosen_address_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Derive the recipient from the caller (`msg.sender`) or from a per-user withdrawal map.\n        //   Use pull-payments: credit an internal balance, let users claim it themselves.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/fund-recovery/SKILL.md",
      "skill_checklist": [
        "No value-moving call takes its destination from instruction arguments without deriving it from"
      ],
      "cwe": "CWE-284",
      "tags": [
        "arbitrary-send",
        "value-flow",
        "SD-002"
      ],
      "corroborated_by": [
        "AC-001 (SG-EVM-006)",
        "CEI-001 (SG-EVM-007)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-SOL-008",
      "detector": "solana_signer",
      "rule": "SIGNER-001",
      "title": "Missing signer check on `reporter` in slash()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.88,
      "score": 6.188,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 185,
        "end_line": 185,
        "function": "slash",
        "contract": "lib.rs"
      },
      "description": "`reporter` is declared as UncheckedAccount<info> in the `Slash` accounts struct, so Anchor cannot prove it was signed. slash() still uses it to authorise a privileged decision. On Solana an unsigned account is just bytes anyone can craft: an attacker supplies their own account with the right key/shape and the program accepts the instruction. The handler contains no `is_signer` check either and no `Signer` account exists in the struct at all.",
      "evidence": "pub reporter: UncheckedAccount<'info>,",
      "exploit_sketch": [
        "Build the instruction with a self-created account in the `reporter` slot (it does not need the real owner's key pair). ",
        "Send the transaction - the program sees a well-formed account and no signature requirement fails.",
        "Repeat for every victim's data the handler writes through that account."
      ],
      "patch_sketch": [
        "Type it as `Signer<'info>`: `reporter: Signer<'info>` (Anchor enforces the signature).",
        "Or add an explicit `require!(ctx.accounts.reporter.is_signer, VaultError::MissingSigner);`.",
        "Better: bind it to state with `has_one = reporter` plus a PDA `seeds` constraint."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-008_sg_sol_008_missing_signer_check_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_008_missing_signer_check_on_reporter_in_s",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-008 - Missing signer check on `reporter` in slash()\n// Severity: high (confidence 0.88)\n// Rule    : SIGNER-001\n// Skill   : skills/solana-signer/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:185 (slash)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_008_missing_signer_check_on_reporter_in_s\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ slash };\n// use vault::{Accounts, lib.rs};\n\n/// PoC harness for SG-SOL-008 - Missing signer check on `reporter` in slash()\n#[tokio::test]\nasync fn sg_sol_008_missing_signer_check_on_reporter_in_s() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Build the instruction with a self-created account in the `reporter` slot (it does not need the real owner's key pair). \n    // Step 2 - Send the transaction - the program sees a well-formed account and no signature requirement fails.\n    // Step 3 - Repeat for every victim's data the handler writes through that account.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-008: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_008_missing_signer_check_on_reporter_in_s_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Type it as `Signer<'info>`: `reporter: Signer<'info>` (Anchor enforces the signature).\n    //   Or add an explicit `require!(ctx.accounts.reporter.is_signer, VaultError::MissingSigner);`.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-signer/SKILL.md",
      "skill_checklist": [
        "Every account whose **signature** is required is typed `Signer<'info>`; the type system is",
        "The key stored as the future authority (`vault.authority = authority.key()`) comes from a"
      ],
      "cwe": "CWE-287",
      "tags": [
        "solana",
        "missing-signer",
        "SIGNER-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-009",
      "detector": "solana_signer",
      "rule": "SIGNER-001",
      "title": "Missing signer check on `authority` in set_limiter()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.88,
      "score": 6.188,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 199,
        "end_line": 199,
        "function": "set_limiter",
        "contract": "lib.rs"
      },
      "description": "`authority` is declared as AccountInfo<info> in the `SetLimiter` accounts struct, so Anchor cannot prove it was signed. set_limiter() still uses it to authorise a privileged decision. On Solana an unsigned account is just bytes anyone can craft: an attacker supplies their own account with the right key/shape and the program accepts the instruction. The handler contains no `is_signer` check either and no `Signer` account exists in the struct at all.",
      "evidence": "pub authority: AccountInfo<'info>,",
      "exploit_sketch": [
        "Build the instruction with a self-created account in the `authority` slot (it does not need the real owner's key pair). ",
        "Send the transaction - the program sees a well-formed account and no signature requirement fails.",
        "Repeat for every victim's data the handler writes through that account."
      ],
      "patch_sketch": [
        "Type it as `Signer<'info>`: `authority: Signer<'info>` (Anchor enforces the signature).",
        "Or add an explicit `require!(ctx.accounts.authority.is_signer, VaultError::MissingSigner);`.",
        "Better: bind it to state with `has_one = authority` plus a PDA `seeds` constraint."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-009_sg_sol_009_missing_signer_check_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_009_missing_signer_check_on_authority_in_",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-009 - Missing signer check on `authority` in set_limiter()\n// Severity: high (confidence 0.88)\n// Rule    : SIGNER-001\n// Skill   : skills/solana-signer/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:199 (set_limiter)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_009_missing_signer_check_on_authority_in_\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ set_limiter };\n// use vault::{Accounts, lib.rs};\n\n/// PoC harness for SG-SOL-009 - Missing signer check on `authority` in set_limiter()\n#[tokio::test]\nasync fn sg_sol_009_missing_signer_check_on_authority_in_() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Build the instruction with a self-created account in the `authority` slot (it does not need the real owner's key pair). \n    // Step 2 - Send the transaction - the program sees a well-formed account and no signature requirement fails.\n    // Step 3 - Repeat for every victim's data the handler writes through that account.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-009: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_009_missing_signer_check_on_authority_in__after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Type it as `Signer<'info>`: `authority: Signer<'info>` (Anchor enforces the signature).\n    //   Or add an explicit `require!(ctx.accounts.authority.is_signer, VaultError::MissingSigner);`.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-signer/SKILL.md",
      "skill_checklist": [
        "Every account whose **signature** is required is typed `Signer<'info>`; the type system is",
        "The key stored as the future authority (`vault.authority = authority.key()`) comes from a"
      ],
      "cwe": "CWE-287",
      "tags": [
        "solana",
        "missing-signer",
        "SIGNER-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-010",
      "detector": "solana_math",
      "rule": "MATH-001",
      "title": "Precision loss: integer division before multiplication in deposit()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.85,
      "score": 6.11,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 31,
        "end_line": 31,
        "function": "deposit",
        "contract": ""
      },
      "description": "deposit() computes `(amount / vault.total_deposits) * 1_000_000_000`. Rust integers truncate, so dividing first throws away the remainder before the scaling factor is applied: for `amount < divisor` the result is exactly zero, and for larger amounts the user loses up to `divisor-1` units of precision per operation. This is the classic share-accounting bug - deposits silently credit 0 shares.",
      "evidence": "let shares = (amount / vault.total_deposits) * 1_000_000_000; // precision loss",
      "exploit_sketch": [
        "Deposit an amount smaller than the divisor (`total_deposits` here) and receive 0 shares.",
        "Do it repeatedly: the pool's recorded deposits grow while your share balance stays at zero - or, inverted, you can take shares that were never paid for.",
        "Round the other way at withdrawal to extract the residual dust on every user."
      ],
      "patch_sketch": [
        "Multiply first, divide last: `let shares = amount.checked_mul(SCALE)?.checked_div(total)?;`",
        "Use `u128` intermediates (`(amount as u128 * SCALE / total) as u64`).",
        "Track a virtual offset (ERC-4626 style inflation guard) so dust cannot be free-minted."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-010_sg_sol_010_precision_loss_intege.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_010_precision_loss_integer_division_befor",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-010 - Precision loss: integer division before multiplication in deposit()\n// Severity: high (confidence 0.85)\n// Rule    : MATH-001\n// Skill   : skills/solana-math/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:31 (deposit)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_010_precision_loss_integer_division_befor\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ deposit };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-010 - Precision loss: integer division before multiplication in deposit()\n#[tokio::test]\nasync fn sg_sol_010_precision_loss_integer_division_befor() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Deposit an amount smaller than the divisor (`total_deposits` here) and receive 0 shares.\n    // Step 2 - Do it repeatedly: the pool's recorded deposits grow while your share balance stays at zero - or, inverted, you can take shares that were never paid for.\n    // Step 3 - Round the other way at withdrawal to extract the residual dust on every user.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-010: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_010_precision_loss_integer_division_befor_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Multiply first, divide last: `let shares = amount.checked_mul(SCALE)?.checked_div(total)?;`\n    //   Use `u128` intermediates (`(amount as u128 * SCALE / total) as u64`).\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-math/SKILL.md",
      "skill_checklist": [
        "Multiplication happens **before** division; division-first expressions are precision-loss",
        "Rounding direction is deliberate and always favours the pool (virtual offset / ERC-4626-style"
      ],
      "cwe": "CWE-682",
      "tags": [
        "solana",
        "precision",
        "accounting",
        "MATH-001"
      ],
      "corroborated_by": [
        "ACCT-001 (SG-SOL-012)",
        "ACCT-002 (SG-SOL-006)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-011",
      "detector": "solana_cpi",
      "rule": "CPI-001",
      "title": "Unchecked CPI: result of `program::invoke` discarded in settle_external()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.85,
      "score": 6.11,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 94,
        "end_line": 94,
        "function": "settle_external",
        "contract": ""
      },
      "description": "settle_external() performs a cross-program invocation and throws the `Result` away (no `?`, no `require!`). If the callee reverts, the CPI error is swallowed and this handler still returns `Ok(())`, so the program records the external operation as if it succeeded. Every downstream accounting decision then runs on a state that never happened.",
      "evidence": "anchor_lang::solana_program::program::invoke(&instruction, &account_infos);",
      "exploit_sketch": [
        "Force the callee to fail (insufficient funds, wrong owner, frozen account).",
        "The CPI error never propagates; settle_external() returns success.",
        "The program's own bookkeeping now diverges from on-chain reality - withdraw against it."
      ],
      "patch_sketch": [
        "Propagate: `program::invoke(&instruction, &account_infos)?;`",
        "Or `require!(result.is_ok(), VaultError::CpiFailed);` with a real error variant.",
        "Return the CPI error unchanged so clients see why the transaction failed."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-011_sg_sol_011_unchecked_cpi_result_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_011_unchecked_cpi_result_of_program_invok",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-011 - Unchecked CPI: result of `program::invoke` discarded in settle_external()\n// Severity: high (confidence 0.85)\n// Rule    : CPI-001\n// Skill   : skills/solana-cpi/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:94 (settle_external)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_011_unchecked_cpi_result_of_program_invok\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ settle_external };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-011 - Unchecked CPI: result of `program::invoke` discarded in settle_external()\n#[tokio::test]\nasync fn sg_sol_011_unchecked_cpi_result_of_program_invok() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Force the callee to fail (insufficient funds, wrong owner, frozen account).\n    // Step 2 - The CPI error never propagates; settle_external() returns success.\n    // Step 3 - The program's own bookkeeping now diverges from on-chain reality - withdraw against it.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-011: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_011_unchecked_cpi_result_of_program_invok_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Propagate: `program::invoke(&instruction, &account_infos)?;`\n    //   Or `require!(result.is_ok(), VaultError::CpiFailed);` with a real error variant.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-cpi/SKILL.md",
      "skill_checklist": [
        "Every CPI result is propagated (`?`) or explicitly handled; a dropped `Result` means the"
      ],
      "cwe": "CWE-252",
      "tags": [
        "solana",
        "cpi",
        "unchecked",
        "CPI-001"
      ],
      "corroborated_by": [
        "ACCT-001 (SG-SOL-013)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-EVM-010",
      "detector": "evm_external_calls",
      "rule": "EC-001",
      "title": "Unchecked value transfer result in claimTreasuryGrant()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.85,
      "score": 6.11,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 89,
        "end_line": 89,
        "function": "claimTreasuryGrant",
        "contract": "HyperVault"
      },
      "description": "claimTreasuryGrant() calls out to e(msg.sender) and ignores the result. e(msg.sender).transfer() can fail silently: low-level `call` returns false instead of reverting, and `send`/`transfer` return false when the recipient cannot accept the value. State that was already updated before the call is not rolled back, so the contract's view of itself diverges from the chain - for accounting code that is a direct loss of user funds.",
      "evidence": "require(signer == admin, \"hyprivault: bad signature\");",
      "exploit_sketch": [
        "Make the callee fail on purpose: revert, run out of gas, or return false (non-standard ERC20).",
        "Trigger claimTreasuryGrant() so the accounting step succeeds while the value move fails.",
        "Withdraw/claim again with the state still advanced."
      ],
      "patch_sketch": [
        "`(bool ok, ) = target.call{value: v}(\"\"); require(ok, \"transfer failed\");`",
        "Or use OpenZeppelin Address.sendValue with an explicit revert.",
        "For non-standard ERC20s use `SafeERC20.safeTransfer` instead of `transfer`."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-010_sg_evm_010_unchecked_value_trans.t.sol",
        "how_to_run": "forge test --match-contract SgEvm010UncheckedValueTransferResultInClHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-010 - Unchecked value transfer result in claimTreasuryGrant()\n// Severity: high (confidence 0.85)\n// Rule    : EC-001\n// Skill   : skills/external-calls/SKILL.md\n// Location: solidity/HyperVault.sol:89 (claimTreasuryGrant)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm010UncheckedValueTransferResultInClHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm010UncheckedValueTransferResultInClHarness\n/// @notice PoC harness for SG-EVM-010.\ncontract SgEvm010UncheckedValueTransferResultInClHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Unchecked value transfer result in claimTreasuryGrant()\n    function test_sg_evm_010_unchecked_value_transfer_result_in_cl() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Make the callee fail on purpose: revert, run out of gas, or return false (non-standard ERC20).\n        // Step 2 - Trigger claimTreasuryGrant() so the accounting step succeeds while the value move fails.\n        // Step 3 - Withdraw/claim again with the state still advanced.\n\n        vm.prank(attacker);\n        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_010_unchecked_value_transfer_result_in_cl_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   `(bool ok, ) = target.call{value: v}(\"\"); require(ok, \"transfer failed\");`\n        //   Or use OpenZeppelin Address.sendValue with an explicit revert.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/external-calls/SKILL.md",
      "skill_checklist": [
        "Non-standard ERC20s (USDT-style no-return, fee-on-transfer, rebasing) are handled with"
      ],
      "cwe": "CWE-252",
      "tags": [
        "unchecked-call",
        "error-handling",
        "EC-001"
      ],
      "corroborated_by": [
        "CEI-001 (SG-EVM-003)",
        "SR-001 (SG-EVM-017)",
        "SR-002 (SG-EVM-002)",
        "SR-003 (SG-EVM-020)",
        "SR-004 (SG-EVM-023)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-011",
      "detector": "evm_access",
      "rule": "AC-002",
      "title": "tx.origin used for authorisation in setOperator()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.85,
      "score": 6.11,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 94,
        "end_line": 94,
        "function": "setOperator",
        "contract": "Vault"
      },
      "description": "setOperator() authorises the caller with `tx.origin` instead of `msg.sender`. tx.origin is the EOA that started the transaction and it keeps equaling the owner even when the owner is tricked into calling a malicious contract first. Any phishing-style interaction therefore hands over full authorisation of this function, which is exactly why Solidity deprecated the pattern.",
      "evidence": "require(tx.origin == owner, \"vault: not owner (tx.origin)\");",
      "exploit_sketch": [
        "Lure the authorised EOA into calling a contract you control (a small incentive is enough).",
        "From that contract's fallback, call setOperator(): `tx.origin == owner` still holds.",
        "Authorisation is granted while `msg.sender` is your contract."
      ],
      "patch_sketch": [
        "Compare against `msg.sender`.",
        "If anti-contract phishing resistance is genuinely wanted, add `require(tx.origin == msg.sender)` *in addition to* the sender check, never instead of it."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-011_sg_evm_011_tx_origin_used_for_au.t.sol",
        "how_to_run": "forge test --match-contract SgEvm011TxOriginUsedForAuthorisationInSeHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-011 - tx.origin used for authorisation in setOperator()\n// Severity: high (confidence 0.85)\n// Rule    : AC-002\n// Skill   : skills/access-control/SKILL.md\n// Location: solidity/Vault.sol:94 (setOperator)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm011TxOriginUsedForAuthorisationInSeHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm011TxOriginUsedForAuthorisationInSeHarness\n/// @notice PoC harness for SG-EVM-011.\ncontract SgEvm011TxOriginUsedForAuthorisationInSeHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice tx.origin used for authorisation in setOperator()\n    function test_sg_evm_011_tx_origin_used_for_authorisation_in_s() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Lure the authorised EOA into calling a contract you control (a small incentive is enough).\n        // Step 2 - From that contract's fallback, call setOperator(): `tx.origin == owner` still holds.\n        // Step 3 - Authorisation is granted while `msg.sender` is your contract.\n\n        vm.prank(attacker);\n        // TODO: call Vault.setOperator(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_011_tx_origin_used_for_authorisation_in_s_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Compare against `msg.sender`.\n        //   If anti-contract phishing resistance is genuinely wanted, add `require(tx.origin == msg.sender)` *in addition to* the sender check, never instead of it.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/access-control/SKILL.md",
      "skill_checklist": [
        "`tx.origin` is **never** the authorisation basis (`msg.sender` is the caller)."
      ],
      "cwe": "CWE-284",
      "tags": [
        "access-control",
        "tx-origin",
        "AC-002"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-012",
      "detector": "evm_external_calls",
      "rule": "EC-001",
      "title": "Unchecked value transfer result in refundPending()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.85,
      "score": 6.11,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 111,
        "end_line": 111,
        "function": "refundPending",
        "contract": "Vault"
      },
      "description": "refundPending() calls out to n and ignores the result. n.transfer() can fail silently: low-level `call` returns false instead of reverting, and `send`/`transfer` return false when the recipient cannot accept the value. State that was already updated before the call is not rolled back, so the contract's view of itself diverges from the chain - for accounting code that is a direct loss of user funds.",
      "evidence": "pendingRewards[user] = 0;",
      "exploit_sketch": [
        "Make the callee fail on purpose: revert, run out of gas, or return false (non-standard ERC20).",
        "Trigger refundPending() so the accounting step succeeds while the value move fails.",
        "Withdraw/claim again with the state still advanced."
      ],
      "patch_sketch": [
        "`(bool ok, ) = target.call{value: v}(\"\"); require(ok, \"transfer failed\");`",
        "Or use OpenZeppelin Address.sendValue with an explicit revert.",
        "For non-standard ERC20s use `SafeERC20.safeTransfer` instead of `transfer`."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-012_sg_evm_012_unchecked_value_trans.t.sol",
        "how_to_run": "forge test --match-contract SgEvm012UncheckedValueTransferResultInReHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-012 - Unchecked value transfer result in refundPending()\n// Severity: high (confidence 0.85)\n// Rule    : EC-001\n// Skill   : skills/external-calls/SKILL.md\n// Location: solidity/Vault.sol:111 (refundPending)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm012UncheckedValueTransferResultInReHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm012UncheckedValueTransferResultInReHarness\n/// @notice PoC harness for SG-EVM-012.\ncontract SgEvm012UncheckedValueTransferResultInReHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Unchecked value transfer result in refundPending()\n    function test_sg_evm_012_unchecked_value_transfer_result_in_re() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Make the callee fail on purpose: revert, run out of gas, or return false (non-standard ERC20).\n        // Step 2 - Trigger refundPending() so the accounting step succeeds while the value move fails.\n        // Step 3 - Withdraw/claim again with the state still advanced.\n\n        vm.prank(attacker);\n        // TODO: call Vault.refundPending(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_012_unchecked_value_transfer_result_in_re_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   `(bool ok, ) = target.call{value: v}(\"\"); require(ok, \"transfer failed\");`\n        //   Or use OpenZeppelin Address.sendValue with an explicit revert.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/external-calls/SKILL.md",
      "skill_checklist": [
        "Non-standard ERC20s (USDT-style no-return, fee-on-transfer, rebasing) are handled with"
      ],
      "cwe": "CWE-252",
      "tags": [
        "unchecked-call",
        "error-handling",
        "EC-001"
      ],
      "corroborated_by": [
        "AC-001 (SG-EVM-019)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-EVM-013",
      "detector": "evm_access",
      "rule": "AC-003",
      "title": "Missing access control on setReserveFactor() (writable: reserveFactorBps)",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.82,
      "score": 6.032,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 94,
        "end_line": 96,
        "function": "setReserveFactor",
        "contract": "HyperVault"
      },
      "description": "setReserveFactor() rewrites security-critical storage (reserveFactorBps) with no authorisation check. An unguarded price feed, fee, cap or reserve parameter is the usual first step of a composed exploit: the attacker does not break the vault directly, they turn the vault's own inputs until a legitimate code path pays them. On HyperEVM treat HyperCore-style reads with the same caution.",
      "evidence": "function setReserveFactor(uint256 bps) external {",
      "exploit_sketch": [
        "Call setReserveFactor() to install a price/parameter you control.",
        "Then trigger the normal accounting path (mint, redeem, liquidate) that reads it."
      ],
      "patch_sketch": [
        "Guard with `onlyOwner`/role, plus an `Upgraded`/`ParamSet` event.",
        "Add a max-move bound and a timelock for security-critical values.",
        "Prefer AccessControl roles over a single `owner` address."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-013_sg_evm_013_missing_access_contro.t.sol",
        "how_to_run": "forge test --match-contract SgEvm013MissingAccessControlOnSetreserveHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-013 - Missing access control on setReserveFactor() (writable: reserveFactorBps)\n// Severity: high (confidence 0.82)\n// Rule    : AC-003\n// Skill   : skills/access-control/SKILL.md\n// Location: solidity/HyperVault.sol:94 (setReserveFactor)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm013MissingAccessControlOnSetreserveHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm013MissingAccessControlOnSetreserveHarness\n/// @notice PoC harness for SG-EVM-013.\ncontract SgEvm013MissingAccessControlOnSetreserveHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Missing access control on setReserveFactor() (writable: reserveFactorBps)\n    function test_sg_evm_013_missing_access_control_on_setreservef() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Call setReserveFactor() to install a price/parameter you control.\n        // Step 2 - Then trigger the normal accounting path (mint, redeem, liquidate) that reads it.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call HyperVault.setReserveFactor(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_013_missing_access_control_on_setreservef_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Guard with `onlyOwner`/role, plus an `Upgraded`/`ParamSet` event.\n        //   Add a max-move bound and a timelock for security-critical values.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/access-control/SKILL.md",
      "skill_checklist": [
        "Each privileged function has either a modifier (`onlyOwner`, `onlyRole`, `onlyAdmin`)",
        "Build the list of externally callable functions and mark which ones mutate privileged state."
      ],
      "cwe": "CWE-284",
      "tags": [
        "access-control",
        "config",
        "AC-003"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-014",
      "detector": "evm_oracle",
      "rule": "OR-002",
      "title": "Oracle round read without staleness/round checks in assetValue()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.82,
      "score": 6.032,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 121,
        "end_line": 121,
        "function": "assetValue",
        "contract": "Vault"
      },
      "description": "assetValue() takes the answer from `latestRoundData()` but never inspects `updatedAt`, `startedAt`, `answeredInRound` or `roundId`. A stale round, a round still in progress, or a negative/zero answer therefore passes straight through, and the contract keeps pricing off a print that may be hours old. The `require(answer > 0)` guard alone is not a freshness check.",
      "evidence": "(, int256 answer, , , ) = priceFeed.latestRoundData();",
      "exploit_sketch": [
        "Wait for the feed to stop updating (heartbeat exceeded) during volatility, or during a keeper outage.",
        "Trade the underlying asset so the market moves while the contract still uses the old print.",
        "Borrow/liquidate against the stale valuation."
      ],
      "patch_sketch": [
        "Require `updatedAt != 0 && block.timestamp - updatedAt <= MAX_STALENESS_SECONDS`.",
        "Require `answeredInRound >= roundId` and `answer > 0`.",
        "Fall back to a second feed (or pause) when the check fails."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-014_sg_evm_014_oracle_round_read_wit.t.sol",
        "how_to_run": "forge test --match-contract SgEvm014OracleRoundReadWithoutStalenessRHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-014 - Oracle round read without staleness/round checks in assetValue()\n// Severity: high (confidence 0.82)\n// Rule    : OR-002\n// Skill   : skills/oracle-price/SKILL.md\n// Location: solidity/Vault.sol:121 (assetValue)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm014OracleRoundReadWithoutStalenessRHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm014OracleRoundReadWithoutStalenessRHarness\n/// @notice PoC harness for SG-EVM-014.\ncontract SgEvm014OracleRoundReadWithoutStalenessRHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Oracle round read without staleness/round checks in assetValue()\n    function test_sg_evm_014_oracle_round_read_without_staleness_r() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Wait for the feed to stop updating (heartbeat exceeded) during volatility, or during a keeper outage.\n        // Step 2 - Trade the underlying asset so the market moves while the contract still uses the old print.\n        // Step 3 - Borrow/liquidate against the stale valuation.\n\n        vm.prank(attacker);\n        // TODO: call Vault.assetValue(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_014_oracle_round_read_without_staleness_r_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Require `updatedAt != 0 && block.timestamp - updatedAt <= MAX_STALENESS_SECONDS`.\n        //   Require `answeredInRound >= roundId` and `answer > 0`.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/oracle-price/SKILL.md",
      "skill_checklist": [
        "Chainlink reads validate all four of: `updatedAt` freshness, `answeredInRound >= roundId`,"
      ],
      "cwe": "CWE-1231",
      "tags": [
        "oracle",
        "staleness",
        "OR-002"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-015",
      "detector": "evm_access",
      "rule": "AC-003",
      "title": "Missing access control on setPriceFeed() (writable: priceFeed)",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.82,
      "score": 6.032,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 130,
        "end_line": 132,
        "function": "setPriceFeed",
        "contract": "Vault"
      },
      "description": "setPriceFeed() rewrites security-critical storage (priceFeed) with no authorisation check. An unguarded price feed, fee, cap or reserve parameter is the usual first step of a composed exploit: the attacker does not break the vault directly, they turn the vault's own inputs until a legitimate code path pays them. On this chain treat HyperCore-style reads with the same caution.",
      "evidence": "function setPriceFeed(address feed) external {",
      "exploit_sketch": [
        "Call setPriceFeed() to install a price/parameter you control.",
        "Then trigger the normal accounting path (mint, redeem, liquidate) that reads it."
      ],
      "patch_sketch": [
        "Guard with `onlyOwner`/role, plus an `Upgraded`/`ParamSet` event.",
        "Add a max-move bound and a timelock for security-critical values.",
        "Prefer AccessControl roles over a single `owner` address."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-015_sg_evm_015_missing_access_contro.t.sol",
        "how_to_run": "forge test --match-contract SgEvm015MissingAccessControlOnSetpricefeHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-015 - Missing access control on setPriceFeed() (writable: priceFeed)\n// Severity: high (confidence 0.82)\n// Rule    : AC-003\n// Skill   : skills/access-control/SKILL.md\n// Location: solidity/Vault.sol:130 (setPriceFeed)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm015MissingAccessControlOnSetpricefeHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm015MissingAccessControlOnSetpricefeHarness\n/// @notice PoC harness for SG-EVM-015.\ncontract SgEvm015MissingAccessControlOnSetpricefeHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Missing access control on setPriceFeed() (writable: priceFeed)\n    function test_sg_evm_015_missing_access_control_on_setpricefee() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Call setPriceFeed() to install a price/parameter you control.\n        // Step 2 - Then trigger the normal accounting path (mint, redeem, liquidate) that reads it.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call Vault.setPriceFeed(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_015_missing_access_control_on_setpricefee_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Guard with `onlyOwner`/role, plus an `Upgraded`/`ParamSet` event.\n        //   Add a max-move bound and a timelock for security-critical values.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/access-control/SKILL.md",
      "skill_checklist": [
        "Each privileged function has either a modifier (`onlyOwner`, `onlyRole`, `onlyAdmin`)",
        "Build the list of externally callable functions and mark which ones mutate privileged state."
      ],
      "cwe": "CWE-284",
      "tags": [
        "access-control",
        "config",
        "AC-003"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-012",
      "detector": "solana_accounts",
      "rule": "ACCT-001",
      "title": "Unvalidated account type: `user_token_account` is UncheckedAccount in Deposit",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.8,
      "score": 5.98,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 154,
        "end_line": 154,
        "function": "deposit",
        "contract": ""
      },
      "description": "`user_token_account` in the `Deposit` constraint list is typed as UncheckedAccount<info>, so Anchor performs no deserialisation, owner or discriminator check on it - yet deposit() passes it into program logic. That is account-confusion territory: an attacker can substitute a look-alike account (wrong owner, wrong mint, wrong program) and the handler has no way to notice. Typing it as `Account<'info, T>` / `Program<'info, T>` makes the runtime enforce it for free.",
      "evidence": "pub user_token_account: UncheckedAccount<'info>,",
      "exploit_sketch": [
        "Create any account with a matching shape (or reuse one you own) and place it in the `user_token_account` slot.",
        "Because ownership/discriminator are not verified, the handler treats it as trusted.",
        "Combine with the missing signer path to redirect value."
      ],
      "patch_sketch": [
        "Use a concrete type: `Account<'info, TokenAccount>` / `Program<'info, Token>` / `Account<'info, VaultPda>`.",
        "Or validate by hand: `require!(acc.owner == &anchor_spl::token::ID, ...)` and check the discriminator/length.",
        "Add `#[account(..., token::mint = expected_mint)]` style constraints for SPL accounts."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-012_sg_sol_012_unvalidated_account_t.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_012_unvalidated_account_type_user_token_a",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-012 - Unvalidated account type: `user_token_account` is UncheckedAccount in Deposit\n// Severity: high (confidence 0.80)\n// Rule    : ACCT-001\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:154 (deposit)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_012_unvalidated_account_type_user_token_a\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ deposit };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-012 - Unvalidated account type: `user_token_account` is UncheckedAccount in Deposit\n#[tokio::test]\nasync fn sg_sol_012_unvalidated_account_type_user_token_a() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Create any account with a matching shape (or reuse one you own) and place it in the `user_token_account` slot.\n    // Step 2 - Because ownership/discriminator are not verified, the handler treats it as trusted.\n    // Step 3 - Combine with the missing signer path to redirect value.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-012: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_012_unvalidated_account_type_user_token_a_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Use a concrete type: `Account<'info, TokenAccount>` / `Program<'info, Token>` / `Account<'info, VaultPda>`.\n    //   Or validate by hand: `require!(acc.owner == &anchor_spl::token::ID, ...)` and check the discriminator/length.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority",
        "Every account that authorises a privileged action is typed `Signer<'info>` (or its"
      ],
      "cwe": "CWE-863",
      "tags": [
        "solana",
        "account-validation",
        "type-confusion",
        "ACCT-001"
      ],
      "corroborated_by": [
        "MATH-001 (SG-SOL-010)",
        "MATH-002 (SG-SOL-022)",
        "MATH-003 (SG-SOL-016)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-013",
      "detector": "solana_accounts",
      "rule": "ACCT-001",
      "title": "Unvalidated account type: `signer_program` is UncheckedAccount in SettleExternal",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.8,
      "score": 5.98,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 208,
        "end_line": 208,
        "function": "settle_external",
        "contract": ""
      },
      "description": "`signer_program` in the `SettleExternal` constraint list is typed as UncheckedAccount<info>, so Anchor performs no deserialisation, owner or discriminator check on it - yet settle_external() passes it into program logic. That is account-confusion territory: an attacker can substitute a look-alike account (wrong owner, wrong mint, wrong program) and the handler has no way to notice. Typing it as `Account<'info, T>` / `Program<'info, T>` makes the runtime enforce it for free.",
      "evidence": "pub signer_program: UncheckedAccount<'info>,",
      "exploit_sketch": [
        "Create any account with a matching shape (or reuse one you own) and place it in the `signer_program` slot.",
        "Because ownership/discriminator are not verified, the handler treats it as trusted.",
        "Combine with the missing signer path to redirect value."
      ],
      "patch_sketch": [
        "Use a concrete type: `Account<'info, TokenAccount>` / `Program<'info, Token>` / `Account<'info, VaultPda>`.",
        "Or validate by hand: `require!(acc.owner == &anchor_spl::token::ID, ...)` and check the discriminator/length.",
        "Add `#[account(..., token::mint = expected_mint)]` style constraints for SPL accounts."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-013_sg_sol_013_unvalidated_account_t.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_013_unvalidated_account_type_signer_progr",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-013 - Unvalidated account type: `signer_program` is UncheckedAccount in SettleExternal\n// Severity: high (confidence 0.80)\n// Rule    : ACCT-001\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:208 (settle_external)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_013_unvalidated_account_type_signer_progr\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ settle_external };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-013 - Unvalidated account type: `signer_program` is UncheckedAccount in SettleExternal\n#[tokio::test]\nasync fn sg_sol_013_unvalidated_account_type_signer_progr() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Create any account with a matching shape (or reuse one you own) and place it in the `signer_program` slot.\n    // Step 2 - Because ownership/discriminator are not verified, the handler treats it as trusted.\n    // Step 3 - Combine with the missing signer path to redirect value.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-013: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_013_unvalidated_account_type_signer_progr_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Use a concrete type: `Account<'info, TokenAccount>` / `Program<'info, Token>` / `Account<'info, VaultPda>`.\n    //   Or validate by hand: `require!(acc.owner == &anchor_spl::token::ID, ...)` and check the discriminator/length.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority",
        "Every account that authorises a privileged action is typed `Signer<'info>` (or its"
      ],
      "cwe": "CWE-863",
      "tags": [
        "solana",
        "account-validation",
        "type-confusion",
        "ACCT-001"
      ],
      "corroborated_by": [
        "CPI-001 (SG-SOL-011)",
        "CPI-002 (SG-SOL-007)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-014",
      "detector": "solana_accounts",
      "rule": "ACCT-001",
      "title": "Unvalidated account type: `raw_config` is AccountInfo in LoadConfig",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.8,
      "score": 5.98,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 217,
        "end_line": 217,
        "function": "load_foreign_config",
        "contract": ""
      },
      "description": "`raw_config` in the `LoadConfig` constraint list is typed as AccountInfo<info>, so Anchor performs no deserialisation, owner or discriminator check on it - yet load_foreign_config() passes it into program logic. That is account-confusion territory: an attacker can substitute a look-alike account (wrong owner, wrong mint, wrong program) and the handler has no way to notice. Typing it as `Account<'info, T>` / `Program<'info, T>` makes the runtime enforce it for free.",
      "evidence": "pub raw_config: AccountInfo<'info>,",
      "exploit_sketch": [
        "Create any account with a matching shape (or reuse one you own) and place it in the `raw_config` slot.",
        "Because ownership/discriminator are not verified, the handler treats it as trusted.",
        "Combine with the missing signer path to redirect value."
      ],
      "patch_sketch": [
        "Use a concrete type: `Account<'info, TokenAccount>` / `Program<'info, Token>` / `Account<'info, VaultPda>`.",
        "Or validate by hand: `require!(acc.owner == &anchor_spl::token::ID, ...)` and check the discriminator/length.",
        "Add `#[account(..., token::mint = expected_mint)]` style constraints for SPL accounts."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-014_sg_sol_014_unvalidated_account_t.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_014_unvalidated_account_type_raw_config_i",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-014 - Unvalidated account type: `raw_config` is AccountInfo in LoadConfig\n// Severity: high (confidence 0.80)\n// Rule    : ACCT-001\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:217 (load_foreign_config)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_014_unvalidated_account_type_raw_config_i\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ load_foreign_config };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-014 - Unvalidated account type: `raw_config` is AccountInfo in LoadConfig\n#[tokio::test]\nasync fn sg_sol_014_unvalidated_account_type_raw_config_i() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Create any account with a matching shape (or reuse one you own) and place it in the `raw_config` slot.\n    // Step 2 - Because ownership/discriminator are not verified, the handler treats it as trusted.\n    // Step 3 - Combine with the missing signer path to redirect value.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-014: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_014_unvalidated_account_type_raw_config_i_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Use a concrete type: `Account<'info, TokenAccount>` / `Program<'info, Token>` / `Account<'info, VaultPda>`.\n    //   Or validate by hand: `require!(acc.owner == &anchor_spl::token::ID, ...)` and check the discriminator/length.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority",
        "Every account that authorises a privileged action is typed `Signer<'info>` (or its"
      ],
      "cwe": "CWE-863",
      "tags": [
        "solana",
        "account-validation",
        "type-confusion",
        "ACCT-001"
      ],
      "corroborated_by": [
        "DESER-001 (SG-SOL-001)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-EVM-016",
      "detector": "evm_delegatecall",
      "rule": "DC-002",
      "title": "Unguarded write to proxy logic pointer `implementation`",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.8,
      "score": 5.98,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 61,
        "end_line": 61,
        "function": "setImplementation",
        "contract": "HyperVault"
      },
      "description": "setImplementation() assigns `implementation` with no authorisation check. Whoever controls that slot controls the logic every delegatecall in this proxy resolves to, which is equivalent to owning the contract and all of its storage. This is the storage-collision/upgrade footgun class: the slot is also shared with the implementation's own layout, so ordering matters.",
      "evidence": "implementation = newImpl;",
      "exploit_sketch": [
        "Point `implementation` at a contract you wrote.",
        "Trigger the fallback/delegate path - all subsequent calls run your code in this storage."
      ],
      "patch_sketch": [
        "Guard with `onlyProxyAdmin` + AccessControl role, and emit an Upgraded event.",
        "Use EIP-1967/ERC-1967 upgradeable proxies from OpenZeppelin rather than a hand-rolled slot.",
        "Keep implementation state in a namespaced storage gap to avoid collisions."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-016_sg_evm_016_unguarded_write_to_pr.t.sol",
        "how_to_run": "forge test --match-contract SgEvm016UnguardedWriteToProxyLogicPointeHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-016 - Unguarded write to proxy logic pointer `implementation`\n// Severity: high (confidence 0.80)\n// Rule    : DC-002\n// Skill   : skills/delegatecall-proxy/SKILL.md\n// Location: solidity/HyperVault.sol:61 (setImplementation)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm016UnguardedWriteToProxyLogicPointeHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm016UnguardedWriteToProxyLogicPointeHarness\n/// @notice PoC harness for SG-EVM-016.\ncontract SgEvm016UnguardedWriteToProxyLogicPointeHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Unguarded write to proxy logic pointer `implementation`\n    function test_sg_evm_016_unguarded_write_to_proxy_logic_pointe() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Point `implementation` at a contract you wrote.\n        // Step 2 - Trigger the fallback/delegate path - all subsequent calls run your code in this storage.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call HyperVault.setImplementation(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_016_unguarded_write_to_proxy_logic_pointe_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Guard with `onlyProxyAdmin` + AccessControl role, and emit an Upgraded event.\n        //   Use EIP-1967/ERC-1967 upgradeable proxies from OpenZeppelin rather than a hand-rolled slot.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/delegatecall-proxy/SKILL.md",
      "skill_checklist": [
        "Writes to logic pointers are guarded by an admin role **and** a timelock/delay, and emit an"
      ],
      "cwe": "CWE-471",
      "tags": [
        "delegatecall",
        "proxy",
        "storage-collision",
        "DC-002"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-017",
      "detector": "evm_sig_replay",
      "rule": "SR-001",
      "title": "Unbound signature digest in claimTreasuryGrant() (cross-chain / cross-contract replay)",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.8,
      "score": 5.98,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 88,
        "end_line": 88,
        "function": "claimTreasuryGrant",
        "contract": "HyperVault"
      },
      "description": "claimTreasuryGrant() verifies a signature with raw `ecrecover` on a caller-supplied hash. The digest does not commit to the verifying contract address or the chain id (no EIP-712 domain separator), so one valid signature is valid everywhere the same bytecode is deployed - every fork, every testnet-to-mainnet copy, and every sibling contract that shares the signer.",
      "evidence": "address signer = ecrecover(doc, v, r, s);",
      "exploit_sketch": [
        "Capture a legitimate signed payload on chain A (or from any off-chain consumer of the signer).",
        "Replay it verbatim against the same deployment on chain B / a sibling contract.",
        "No forging is required: the signature is authentic, just in the wrong context."
      ],
      "patch_sketch": [
        "Use OpenZeppelin EIP712 + `ECDSA.recover(typedDigest, v, r, s)` so the domain binds `address(this)` and `block.chainid`.",
        "If hand-rolled, hash `0x19 0x01 domainSeparator structHash` and cache the domain separator including chainid so forks invalidate it."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-017_sg_evm_017_unbound_signature_dig.t.sol",
        "how_to_run": "forge test --match-contract SgEvm017UnboundSignatureDigestInClaimtreHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-017 - Unbound signature digest in claimTreasuryGrant() (cross-chain / cross-contract replay)\n// Severity: high (confidence 0.80)\n// Rule    : SR-001\n// Skill   : skills/signature-replay/SKILL.md\n// Location: solidity/HyperVault.sol:88 (claimTreasuryGrant)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm017UnboundSignatureDigestInClaimtreHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm017UnboundSignatureDigestInClaimtreHarness\n/// @notice PoC harness for SG-EVM-017.\ncontract SgEvm017UnboundSignatureDigestInClaimtreHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Unbound signature digest in claimTreasuryGrant() (cross-chain / cross-contract replay)\n    function test_sg_evm_017_unbound_signature_digest_in_claimtrea() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Capture a legitimate signed payload on chain A (or from any off-chain consumer of the signer).\n        // Step 2 - Replay it verbatim against the same deployment on chain B / a sibling contract.\n        // Step 3 - No forging is required: the signature is authentic, just in the wrong context.\n\n        vm.prank(attacker);\n        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_017_unbound_signature_digest_in_claimtrea_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Use OpenZeppelin EIP712 + `ECDSA.recover(typedDigest, v, r, s)` so the domain binds `address(this)` and `block.chainid`.\n        //   If hand-rolled, hash `0x19 0x01 domainSeparator structHash` and cache the domain separator including chainid so forks invalidate it.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/signature-replay/SKILL.md",
      "skill_checklist": [
        "The digest commits to the verifying context: EIP-712 domain separator with `name`,"
      ],
      "cwe": "CWE-294",
      "tags": [
        "signature",
        "replay",
        "SR-001"
      ],
      "corroborated_by": [
        "CEI-001 (SG-EVM-003)",
        "EC-001 (SG-EVM-010)",
        "EC-003 (SG-EVM-024)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-018",
      "detector": "evm_selfdestruct",
      "rule": "SF-001",
      "title": "Stranded funds: recoverTokens() is gated on `pendingAdmin`, which is never assigned",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.8,
      "score": 5.98,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 109,
        "end_line": 109,
        "function": "recoverTokens",
        "contract": "HyperVault"
      },
      "description": "`pendingAdmin` is declared as a storage address and used as the only authorisation for recoverTokens(), but nothing in this contract ever assigns it. It is therefore zero (`address(0)`), and no caller can ever satisfy the check. The function is dead code, which means value that only this path can move - ETH and tokens held by the contract - is permanently stranded.",
      "evidence": "require(msg.sender == pendingAdmin, \"hyprivault: not pending admin\");",
      "exploit_sketch": [
        "No attacker required: send value to the contract (or let it accumulate) and the recovery path can never be executed.",
        "If the gate is `msg.sender == address(0)`, the check can even be satisfied by a crafted creation flow, turning dead code into an open door."
      ],
      "patch_sketch": [
        "Assign the variable in the constructor or an initialiser, or delete the path.",
        "Add a test asserting the recovery function is callable by the intended role.",
        "Prefer AccessControl roles with `grantRole` at deploy time over bare address variables."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-018_sg_evm_018_stranded_funds_recove.t.sol",
        "how_to_run": "forge test --match-contract SgEvm018StrandedFundsRecovertokensIsGateHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-018 - Stranded funds: recoverTokens() is gated on `pendingAdmin`, which is never assigned\n// Severity: high (confidence 0.80)\n// Rule    : SF-001\n// Skill   : skills/fund-recovery/SKILL.md\n// Location: solidity/HyperVault.sol:109 (recoverTokens)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm018StrandedFundsRecovertokensIsGateHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm018StrandedFundsRecovertokensIsGateHarness\n/// @notice PoC harness for SG-EVM-018.\ncontract SgEvm018StrandedFundsRecovertokensIsGateHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Stranded funds: recoverTokens() is gated on `pendingAdmin`, which is never assigned\n    function test_sg_evm_018_stranded_funds_recovertokens_is_gated() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   No attacker required: send value to the contract (or let it accumulate) and the recovery path can never be executed.\n        // Step 2 - If the gate is `msg.sender == address(0)`, the check can even be satisfied by a crafted creation flow, turning dead code into an open door.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call HyperVault.recoverTokens(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_018_stranded_funds_recovertokens_is_gated_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Assign the variable in the constructor or an initialiser, or delete the path.\n        //   Add a test asserting the recovery function is callable by the intended role.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/fund-recovery/SKILL.md",
      "skill_checklist": [
        "Token recovery uses `SafeERC20` and checks the return data - a rescue that silently fails is"
      ],
      "cwe": "CWE-284",
      "tags": [
        "stuck-funds",
        "unreachable-guard",
        "SF-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-019",
      "detector": "evm_access",
      "rule": "AC-001",
      "title": "Missing access control on refundPending()",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.75,
      "score": 5.85,
      "chain": "evm",
      "location": {
        "file": "solidity/Vault.sol",
        "line": 106,
        "end_line": 114,
        "function": "refundPending",
        "contract": "Vault"
      },
      "description": "refundPending() is public, moves value to `user`, and writes owed, yet nothing in the function proves who the caller is: no `only*` modifier, no `require(msg.sender == ...)`, no role check, and no caller-scoped accounting. So the state it touches is not limited to the caller's own balance - any address can call it. The destination is a named user, so the direct risk is griefing/replay of that user's claim rather than an empty contract.",
      "evidence": "function refundPending(address user) public returns (uint256 paid) {",
      "exploit_sketch": [
        "Call refundPending() from an EOA you control, with the arguments you want.",
        "No key compromise or front-running is required - the entry point is permissionless.",
        "Repeat until the shared balance is gone (or until every victim's claim has been consumed)."
      ],
      "patch_sketch": [
        "Add `onlyOwner` / AccessControl `onlyRole(GUARDIAN_ROLE)` to refundPending().",
        "Or make the function self-service: derive the destination from `msg.sender` and let users pull their own funds.",
        "Emit an event and add a per-call cap so a wrong grant of authority is bounded."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-019_sg_evm_019_missing_access_contro.t.sol",
        "how_to_run": "forge test --match-contract SgEvm019MissingAccessControlOnRefundpendHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-019 - Missing access control on refundPending()\n// Severity: high (confidence 0.75)\n// Rule    : AC-001\n// Skill   : skills/access-control/SKILL.md\n// Location: solidity/Vault.sol:106 (refundPending)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm019MissingAccessControlOnRefundpendHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/Vault.sol\";\n\n/// @title SgEvm019MissingAccessControlOnRefundpendHarness\n/// @notice PoC harness for SG-EVM-019.\ncontract SgEvm019MissingAccessControlOnRefundpendHarness is Test {\n    Vault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new Vault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Missing access control on refundPending()\n    function test_sg_evm_019_missing_access_control_on_refundpendi() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Call refundPending() from an EOA you control, with the arguments you want.\n        // Step 2 - No key compromise or front-running is required - the entry point is permissionless.\n        // Step 3 - Repeat until the shared balance is gone (or until every victim's claim has been consumed).\n\n        vm.prank(attacker);\n        // TODO: call Vault.refundPending(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_019_missing_access_control_on_refundpendi_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Add `onlyOwner` / AccessControl `onlyRole(GUARDIAN_ROLE)` to refundPending().\n        //   Or make the function self-service: derive the destination from `msg.sender` and let users pull their own funds.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/access-control/SKILL.md",
      "skill_checklist": [
        "Each privileged function has either a modifier (`onlyOwner`, `onlyRole`, `onlyAdmin`)",
        "Build the list of externally callable functions and mark which ones mutate privileged state."
      ],
      "cwe": "CWE-284",
      "tags": [
        "access-control",
        "value-flow",
        "AC-001"
      ],
      "corroborated_by": [
        "EC-001 (SG-EVM-012)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-015",
      "detector": "solana_cpi",
      "rule": "CPI-003",
      "title": "SPL token balance edited directly on `vault_token_account` instead of via CPI",
      "severity": "high",
      "severity_weight": 6.5,
      "confidence": 0.7,
      "score": 5.72,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 68,
        "end_line": 68,
        "function": "slash",
        "contract": ""
      },
      "description": "slash() writes `ctx.accounts.vault_token_account.amount` directly. An SPL token account is owned by the token program, so a well-behaved runtime will reject the mutation; where the owner check is weak (or the account was passed as a raw/duplicated account) the balance can be forged inside the transaction, letting an attacker mint purchasing power without moving tokens.",
      "evidence": "ctx.accounts.vault_token_account.amount += shares;",
      "exploit_sketch": [
        "Pass an account the handler treats as the vault token account.",
        "Direct mutation path (or a CPI on an unvalidated program) inflates `amount`.",
        "Withdraw/settle logic trusts `amount` and releases real tokens."
      ],
      "patch_sketch": [
        "Move balances with `token::transfer` / `token::mint_to` CPIs signed by the vault PDA.",
        "Keep program-owned state in your own accounts, never in foreign-owned layouts."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-015_sg_sol_015_spl_token_balance_edi.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_015_spl_token_balance_edited_directly_on_",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-015 - SPL token balance edited directly on `vault_token_account` instead of via CPI\n// Severity: high (confidence 0.70)\n// Rule    : CPI-003\n// Skill   : skills/solana-cpi/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:68 (slash)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_015_spl_token_balance_edited_directly_on_\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ slash };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-015 - SPL token balance edited directly on `vault_token_account` instead of via CPI\n#[tokio::test]\nasync fn sg_sol_015_spl_token_balance_edited_directly_on_() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Pass an account the handler treats as the vault token account.\n    // Step 2 - Direct mutation path (or a CPI on an unvalidated program) inflates `amount`.\n    // Step 3 - Withdraw/settle logic trusts `amount` and releases real tokens.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-015: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_015_spl_token_balance_edited_directly_on__after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Move balances with `token::transfer` / `token::mint_to` CPIs signed by the vault PDA.\n    //   Keep program-owned state in your own accounts, never in foreign-owned layouts.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-cpi/SKILL.md",
      "skill_checklist": [
        "Every CPI result is propagated (`?`) or explicitly handled; a dropped `Result` means the"
      ],
      "cwe": "CWE-252",
      "tags": [
        "solana",
        "spl",
        "account-ownership",
        "CPI-003"
      ],
      "corroborated_by": [
        "MATH-004 (SG-SOL-025)",
        "SIGNER-002 (SG-SOL-004)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-SOL-016",
      "detector": "solana_math",
      "rule": "MATH-003",
      "title": "Division by `vault.total_deposits` with no zero guard in deposit()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.8,
      "score": 3.68,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 31,
        "end_line": 31,
        "function": "deposit",
        "contract": ""
      },
      "description": "deposit() divides by `vault.total_deposits`, which is program state that can legitimately be zero (a fresh vault). On Solana an integer division by zero panics the builtin, the transaction fails with an opaque error, and the first depositor can never complete the flow - an availability bug rather than a theft, but it blocks the account's own happy path.",
      "evidence": "let shares = (amount / vault.total_deposits) * 1_000_000_000; // precision loss",
      "exploit_sketch": [
        "Call the handler while the divisor is zero (empty vault) to force the panic.",
        "If the zero branch is also how shares are seeded, the whole feature is unusable."
      ],
      "patch_sketch": [
        "Special-case the empty pool: `let shares = if total == 0 { amount } else { ... };`",
        "`checked_div(...).ok_or(VaultError::DivideByZero)?` so the failure is a typed error."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-016_sg_sol_016_division_by_vault_tot.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_016_division_by_vault_total_deposits_with",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-016 - Division by `vault.total_deposits` with no zero guard in deposit()\n// Severity: medium (confidence 0.80)\n// Rule    : MATH-003\n// Skill   : skills/solana-math/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:31 (deposit)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_016_division_by_vault_total_deposits_with\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ deposit };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-016 - Division by `vault.total_deposits` with no zero guard in deposit()\n#[tokio::test]\nasync fn sg_sol_016_division_by_vault_total_deposits_with() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Call the handler while the divisor is zero (empty vault) to force the panic.\n    // Step 2 - If the zero branch is also how shares are seeded, the whole feature is unusable.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-016: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_016_division_by_vault_total_deposits_with_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Special-case the empty pool: `let shares = if total == 0 { amount } else { ... };`\n    //   `checked_div(...).ok_or(VaultError::DivideByZero)?` so the failure is a typed error.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-math/SKILL.md",
      "skill_checklist": [
        "Divisors can never be zero: the empty-vault case is special-cased or guarded with"
      ],
      "cwe": "CWE-682",
      "tags": [
        "solana",
        "division-by-zero",
        "availability",
        "MATH-003"
      ],
      "corroborated_by": [
        "ACCT-001 (SG-SOL-012)",
        "ACCT-002 (SG-SOL-006)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-017",
      "detector": "solana_cpi",
      "rule": "REMOT-001",
      "title": "`remaining_accounts` indexed at [0] without a length check",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.75,
      "score": 3.6,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 105,
        "end_line": 105,
        "function": "settle_many",
        "contract": ""
      },
      "description": "settle_many() reads `remaining_accounts[0]` from `ctx.remaining_accounts` before checking how many accounts the client actually sent. A short account list panics on the slice index (an uncaught panic in the handler, not a clean program error), and any validation that *would* have happened afterwards is skipped - note the ordering here: the read precedes the `require!`.",
      "evidence": "let first = &ctx.remaining_accounts[0];",
      "exploit_sketch": [
        "Send the instruction with fewer remaining accounts than expected.",
        "The program panics on the index; clients see an opaque failure (DoS of that path).",
        "Or rely on the missing validation to slip a wrong-role account into position."
      ],
      "patch_sketch": [
        "`ensure!(ctx.remaining_accounts.len() >= 2, VaultError::NotEnoughAccounts);` before indexing.",
        "Iterate with `.iter().next().ok_or(...)` so absence becomes a typed error.",
        "Validate each remaining account's owner/mint - they are outside Anchor's checks."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-017_sg_sol_017_remaining_accounts_in.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_017_remaining_accounts_indexed_at_0_witho",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-017 - `remaining_accounts` indexed at [0] without a length check\n// Severity: medium (confidence 0.75)\n// Rule    : REMOT-001\n// Skill   : skills/solana-cpi/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:105 (settle_many)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_017_remaining_accounts_indexed_at_0_witho\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ settle_many };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-017 - `remaining_accounts` indexed at [0] without a length check\n#[tokio::test]\nasync fn sg_sol_017_remaining_accounts_indexed_at_0_witho() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Send the instruction with fewer remaining accounts than expected.\n    // Step 2 - The program panics on the index; clients see an opaque failure (DoS of that path).\n    // Step 3 - Or rely on the missing validation to slip a wrong-role account into position.\n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-017: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_017_remaining_accounts_indexed_at_0_witho_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   `ensure!(ctx.remaining_accounts.len() >= 2, VaultError::NotEnoughAccounts);` before indexing.\n    //   Iterate with `.iter().next().ok_or(...)` so absence becomes a typed error.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-cpi/SKILL.md",
      "skill_checklist": [
        "`remaining_accounts` lengths are validated before they are used as CPI accounts."
      ],
      "cwe": "CWE-252",
      "tags": [
        "solana",
        "remaining-accounts",
        "panic",
        "REMOT-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-018",
      "detector": "solana_accounts",
      "rule": "ACCT-003",
      "title": "PDA seeds constraint trusts a client-supplied bump",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.75,
      "score": 3.6,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 149,
        "end_line": 149,
        "function": "",
        "contract": ""
      },
      "description": "The account constraint uses `bump = vault_config.bump`, and that stored bump was written from an instruction argument in `initialize`. Anchor will therefore accept *any* bump the client first persisted, so the seeds constraint stops pinning the address to a single canonical PDA. Bump values are part of the address derivation: letting the client choose them is a validation gap.",
      "evidence": "#[account(mut, seeds = [b\"vault\", vault_config.key().as_ref()], bump = vault_config.bump)]",
      "exploit_sketch": [
        "Call `initialize` with a bump that still satisfies the (weak) derivation and store it.",
        "Reuse that stored bump later to make a seeds check accept a non-canonical address."
      ],
      "patch_sketch": [
        "Derive the bump: `bump` (Anchor verifies) or `let (,_ ,bump) = Pubkey::find_program_address(...)`.",
        "Store the *derived* bump, never the client's."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-018_sg_sol_018_pda_seeds_constraint_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_018_pda_seeds_constraint_trusts_a_client_",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-018 - PDA seeds constraint trusts a client-supplied bump\n// Severity: medium (confidence 0.75)\n// Rule    : ACCT-003\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:149\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_018_pda_seeds_constraint_trusts_a_client_\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ vulnerableFunction };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-018 - PDA seeds constraint trusts a client-supplied bump\n#[tokio::test]\nasync fn sg_sol_018_pda_seeds_constraint_trusts_a_client_() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Call `initialize` with a bump that still satisfies the (weak) derivation and store it.\n    // Step 2 - Reuse that stored bump later to make a seeds check accept a non-canonical address.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-018: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_018_pda_seeds_constraint_trusts_a_client__after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Derive the bump: `bump` (Anchor verifies) or `let (,_ ,bump) = Pubkey::find_program_address(...)`.\n    //   Store the *derived* bump, never the client's.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "PDAs are derived, not trusted: `seeds` + `bump` verified by Anchor, or"
      ],
      "cwe": "CWE-863",
      "tags": [
        "solana",
        "pda",
        "seeds",
        "ACCT-003"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-019",
      "detector": "solana_accounts",
      "rule": "ACCT-003",
      "title": "PDA seeds constraint trusts a client-supplied bump",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.75,
      "score": 3.6,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 169,
        "end_line": 169,
        "function": "",
        "contract": ""
      },
      "description": "The account constraint uses `bump = vault_config.bump`, and that stored bump was written from an instruction argument in `initialize`. Anchor will therefore accept *any* bump the client first persisted, so the seeds constraint stops pinning the address to a single canonical PDA. Bump values are part of the address derivation: letting the client choose them is a validation gap.",
      "evidence": "#[account(mut, seeds = [b\"vault\", vault_config.key().as_ref()], bump = vault_config.bump, realloc = 8, realloc::payer = vault_config, realloc::zero = false)]",
      "exploit_sketch": [
        "Call `initialize` with a bump that still satisfies the (weak) derivation and store it.",
        "Reuse that stored bump later to make a seeds check accept a non-canonical address."
      ],
      "patch_sketch": [
        "Derive the bump: `bump` (Anchor verifies) or `let (,_ ,bump) = Pubkey::find_program_address(...)`.",
        "Store the *derived* bump, never the client's."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-019_sg_sol_019_pda_seeds_constraint_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_019_pda_seeds_constraint_trusts_a_client_",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-019 - PDA seeds constraint trusts a client-supplied bump\n// Severity: medium (confidence 0.75)\n// Rule    : ACCT-003\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:169\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_019_pda_seeds_constraint_trusts_a_client_\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ vulnerableFunction };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-019 - PDA seeds constraint trusts a client-supplied bump\n#[tokio::test]\nasync fn sg_sol_019_pda_seeds_constraint_trusts_a_client_() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Call `initialize` with a bump that still satisfies the (weak) derivation and store it.\n    // Step 2 - Reuse that stored bump later to make a seeds check accept a non-canonical address.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-019: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_019_pda_seeds_constraint_trusts_a_client__after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Derive the bump: `bump` (Anchor verifies) or `let (,_ ,bump) = Pubkey::find_program_address(...)`.\n    //   Store the *derived* bump, never the client's.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "PDAs are derived, not trusted: `seeds` + `bump` verified by Anchor, or"
      ],
      "cwe": "CWE-863",
      "tags": [
        "solana",
        "pda",
        "seeds",
        "ACCT-003"
      ],
      "corroborated_by": [
        "AUTH-002 (SG-SOL-024)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-020",
      "detector": "solana_authority",
      "rule": "AUTH-001",
      "title": "PDA authority depends on a client-supplied `bump` in initialize()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.7,
      "score": 3.52,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 17,
        "end_line": 17,
        "function": "initialize",
        "contract": ""
      },
      "description": "initialize() takes `bump: u8` straight from the instruction and stores it, and other handlers then use that stored value as their seeds constraint (`bump = config.bump`). A PDA is only canonical when the bump is derived with `find_program_address`; storing the caller's number means the first writer decides how authority is derived. Even where `init` seeds constrain this particular deployment, the pattern is how upgrade/freeze/mint authority bugs hide.",
      "evidence": "pub fn initialize(ctx: Context<Initialize>, bump: u8, limiter: u64) -> Result<()> {",
      "exploit_sketch": [
        "If the PDA is not uniquely constrained at init, pick a bump that makes a *different* address satisfy the seeds check.",
        "Then present an account derived from that alternative bump to handlers trusting the stored value."
      ],
      "patch_sketch": [
        "Drop the parameter and let Anchor verify the bump (`seeds = [...], bump`).",
        "Or derive it: `let (_pda, bump) = Pubkey::find_program_address(&[b\"vault\", authority.as_ref()], &id());`",
        "If a bump must be stored, assert it equals the derived value on every read."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-020_sg_sol_020_pda_authority_depends.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_020_pda_authority_depends_on_a_client_sup",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-020 - PDA authority depends on a client-supplied `bump` in initialize()\n// Severity: medium (confidence 0.70)\n// Rule    : AUTH-001\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:17 (initialize)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_020_pda_authority_depends_on_a_client_sup\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ initialize };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-020 - PDA authority depends on a client-supplied `bump` in initialize()\n#[tokio::test]\nasync fn sg_sol_020_pda_authority_depends_on_a_client_sup() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - If the PDA is not uniquely constrained at init, pick a bump that makes a *different* address satisfy the seeds check.\n    // Step 2 - Then present an account derived from that alternative bump to handlers trusting the stored value.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-020: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_020_pda_authority_depends_on_a_client_sup_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Drop the parameter and let Anchor verify the bump (`seeds = [...], bump`).\n    //   Or derive it: `let (_pda, bump) = Pubkey::find_program_address(&[b\"vault\", authority.as_ref()], &id());`\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "PDAs are derived, not trusted: `seeds` + `bump` verified by Anchor, or",
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority"
      ],
      "cwe": "CWE-269",
      "tags": [
        "solana",
        "pda",
        "authority",
        "AUTH-001"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-EVM-020",
      "detector": "evm_sig_replay",
      "rule": "SR-003",
      "title": "No signature expiry in claimTreasuryGrant()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.7,
      "score": 3.52,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 87,
        "end_line": 87,
        "function": "claimTreasuryGrant",
        "contract": "HyperVault"
      },
      "description": "claimTreasuryGrant() accepts a signature with no deadline and no expiry check. A signer's intent is usually time-bounded - rates, collateral ratios and allowances change - so a signature that was reasonable on the day it was signed can be catastrophic months later, and a leaked offline payload never expires.",
      "evidence": "function claimTreasuryGrant(bytes32 doc, uint8 v, bytes32 r, bytes32 s, uint256 amount) external {",
      "exploit_sketch": [
        "Collect signed payloads when conditions are favourable.",
        "Hold them until prices/limits move in your favour, then submit."
      ],
      "patch_sketch": [
        "Put `uint256 deadline` in the signed payload and `require(block.timestamp <= deadline, \"expired\")`.",
        "Keep the default TTL short (minutes to hours) and document it for signers."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-020_sg_evm_020_no_signature_expiry_i.t.sol",
        "how_to_run": "forge test --match-contract SgEvm020NoSignatureExpiryInClaimtreasuryHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-020 - No signature expiry in claimTreasuryGrant()\n// Severity: medium (confidence 0.70)\n// Rule    : SR-003\n// Skill   : skills/signature-replay/SKILL.md\n// Location: solidity/HyperVault.sol:87 (claimTreasuryGrant)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm020NoSignatureExpiryInClaimtreasuryHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm020NoSignatureExpiryInClaimtreasuryHarness\n/// @notice PoC harness for SG-EVM-020.\ncontract SgEvm020NoSignatureExpiryInClaimtreasuryHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice No signature expiry in claimTreasuryGrant()\n    function test_sg_evm_020_no_signature_expiry_in_claimtreasuryg() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Collect signed payloads when conditions are favourable.\n        // Step 2 - Hold them until prices/limits move in your favour, then submit.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_020_no_signature_expiry_in_claimtreasuryg_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Put `uint256 deadline` in the signed payload and `require(block.timestamp <= deadline, \"expired\")`.\n        //   Keep the default TTL short (minutes to hours) and document it for signers.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/signature-replay/SKILL.md",
      "skill_checklist": [
        "A `deadline` is part of the signed payload and is enforced against `block.timestamp`."
      ],
      "cwe": "CWE-294",
      "tags": [
        "signature",
        "deadline",
        "SR-003"
      ],
      "corroborated_by": [
        "CEI-001 (SG-EVM-003)",
        "EC-001 (SG-EVM-010)",
        "EC-003 (SG-EVM-024)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-021",
      "detector": "evm_selfdestruct",
      "rule": "SF-003",
      "title": "Token recovery result is never checked in recoverTokens()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.7,
      "score": 3.52,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 110,
        "end_line": 110,
        "function": "recoverTokens",
        "contract": "HyperVault"
      },
      "description": "The recovery path does a low-level `token.call(...)` and then discards the boolean (the code shape here is `(bool ok, ) = ...; ok;`). A non-standard or pausable token that returns false makes the caller believe the rescue succeeded, so a failed recovery looks like a completed one.",
      "evidence": "(bool ok, ) = token.call(abi.encodeWithSelector(0xa9059cbb, to, amount));",
      "exploit_sketch": [
        "Choose a token that returns false instead of reverting (USDT-style) or that is paused.",
        "The recovery call reports success while nothing moved."
      ],
      "patch_sketch": [
        "`require(ok && (abi.decode(ret, (bool)) ), \"transfer failed\")` or use SafeERC20.safeTransfer.",
        "Revert on any zero-length return data as well."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-021_sg_evm_021_token_recovery_result.t.sol",
        "how_to_run": "forge test --match-contract SgEvm021TokenRecoveryResultIsNeverCheckeHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-021 - Token recovery result is never checked in recoverTokens()\n// Severity: medium (confidence 0.70)\n// Rule    : SF-003\n// Skill   : skills/fund-recovery/SKILL.md\n// Location: solidity/HyperVault.sol:110 (recoverTokens)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm021TokenRecoveryResultIsNeverCheckeHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm021TokenRecoveryResultIsNeverCheckeHarness\n/// @notice PoC harness for SG-EVM-021.\ncontract SgEvm021TokenRecoveryResultIsNeverCheckeHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Token recovery result is never checked in recoverTokens()\n    function test_sg_evm_021_token_recovery_result_is_never_checke() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Choose a token that returns false instead of reverting (USDT-style) or that is paused.\n        // Step 2 - The recovery call reports success while nothing moved.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call HyperVault.recoverTokens(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_021_token_recovery_result_is_never_checke_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   `require(ok && (abi.decode(ret, (bool)) ), \"transfer failed\")` or use SafeERC20.safeTransfer.\n        //   Revert on any zero-length return data as well.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/fund-recovery/SKILL.md",
      "skill_checklist": [
        "Token recovery uses `SafeERC20` and checks the return data - a rescue that silently fails is"
      ],
      "cwe": "CWE-284",
      "tags": [
        "unchecked-call",
        "token-recovery",
        "SF-003"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-021",
      "detector": "solana_authority",
      "rule": "AUTH-003",
      "title": "Authority account `user_authority` is used by withdraw() with no signer and no binding",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.65,
      "score": 3.44,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 174,
        "end_line": 174,
        "function": "withdraw",
        "contract": ""
      },
      "description": "`user_authority` in `Withdraw` reads like an authority (AccountInfo<info>) but is not a `Signer`, is not tied by `has_one`/`constraint`/`seeds`, and withdraw() still consumes it. Any mint / freeze / close authority check the program assumes happens here is really happening on an account the client chose. The privilege exists; nothing in the source limits who exercises it.",
      "evidence": "pub user_authority: AccountInfo<'info>,",
      "exploit_sketch": [
        "Supply a different key in the `user_authority` slot (or reuse an account you own that matches the shape).",
        "Pair it with the missing-signer path in the same handler to complete the authority chain."
      ],
      "patch_sketch": [
        "`Signer<'info>` plus `#[account(constraint = user_authority.key() == expected_authority)]`.",
        "Read SPL authorities from the mint/account data instead of from an unvalidated account.",
        "Use a two-step authority handover so a single bad call cannot transfer ownership."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-021_sg_sol_021_authority_account_use.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_021_authority_account_user_authority_is_u",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-021 - Authority account `user_authority` is used by withdraw() with no signer and no binding\n// Severity: medium (confidence 0.65)\n// Rule    : AUTH-003\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:174 (withdraw)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_021_authority_account_user_authority_is_u\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ withdraw };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-021 - Authority account `user_authority` is used by withdraw() with no signer and no binding\n#[tokio::test]\nasync fn sg_sol_021_authority_account_user_authority_is_u() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Supply a different key in the `user_authority` slot (or reuse an account you own that matches the shape).\n    // Step 2 - Pair it with the missing-signer path in the same handler to complete the authority chain.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-021: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_021_authority_account_user_authority_is_u_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   `Signer<'info>` plus `#[account(constraint = user_authority.key() == expected_authority)]`.\n    //   Read SPL authorities from the mint/account data instead of from an unvalidated account.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority",
        "Every account that authorises a privileged action is typed `Signer<'info>` (or its"
      ],
      "cwe": "CWE-269",
      "tags": [
        "solana",
        "authority",
        "static-note",
        "AUTH-003"
      ],
      "corroborated_by": [
        "MATH-002 (SG-SOL-023)",
        "SIGNER-002 (SG-SOL-003)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-022",
      "detector": "evm_oracle",
      "rule": "OR-003",
      "title": "Single-source price oracle.spotPrice() decides value in priceOf()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.65,
      "score": 3.44,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 74,
        "end_line": 74,
        "function": "priceOf",
        "contract": "HyperVault"
      },
      "description": "priceOf() trusts one oracle call (oracle.spotPrice) for a value-deciding calculation, with no deviation check, no second source and no circuit breaker.On HyperEVM the HyperCore spot read is itself a consensus input, so treat unit/exponent mismatches and asset-alias confusion as the realistic failure mode. If that single print is wrong - manipulated, stale, or a different asset's unit - every dependent decision is wrong in the same direction.",
      "evidence": "return oracle.spotPrice(asset);",
      "exploit_sketch": [
        "Identify the oracle's own trust path (a small set of attesters, a single pair, a low-liquidity venue).",
        "Move or misreport that one input.",
        "Call the value-deciding entry point while the whole system believes the bad number."
      ],
      "patch_sketch": [
        "Cross-check a second independent source and revert if they differ by more than a threshold.",
        "Validate the returned exponent/units before using the number.",
        "Add a per-block max-move (circuit breaker) and an explicit pause role."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-022_sg_evm_022_single_source_price_o.t.sol",
        "how_to_run": "forge test --match-contract SgEvm022SingleSourcePriceOracleSpotpriceHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-022 - Single-source price oracle.spotPrice() decides value in priceOf()\n// Severity: medium (confidence 0.65)\n// Rule    : OR-003\n// Skill   : skills/oracle-price/SKILL.md\n// Location: solidity/HyperVault.sol:74 (priceOf)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm022SingleSourcePriceOracleSpotpriceHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm022SingleSourcePriceOracleSpotpriceHarness\n/// @notice PoC harness for SG-EVM-022.\ncontract SgEvm022SingleSourcePriceOracleSpotpriceHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Single-source price oracle.spotPrice() decides value in priceOf()\n    function test_sg_evm_022_single_source_price_oracle_spotprice_() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Identify the oracle's own trust path (a small set of attesters, a single pair, a low-liquidity venue).\n        // Step 2 - Move or misreport that one input.\n        // Step 3 - Call the value-deciding entry point while the whole system believes the bad number.\n\n        vm.prank(attacker);\n        // TODO: call HyperVault.priceOf(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_022_single_source_price_oracle_spotprice__afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Cross-check a second independent source and revert if they differ by more than a threshold.\n        //   Validate the returned exponent/units before using the number.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/oracle-price/SKILL.md",
      "skill_checklist": [
        "A second, independent source or a max-deviation circuit breaker exists for anything that",
        "Units/exponents are asserted (1e8 Chainlink vs 1e18 pool decimals vs HyperCore asset"
      ],
      "cwe": "CWE-1231",
      "tags": [
        "oracle",
        "single-source",
        "OR-003"
      ],
      "corroborated_by": [],
      "independent_confirmation": 0
    },
    {
      "id": "SG-SOL-022",
      "detector": "solana_math",
      "rule": "MATH-002",
      "title": "Unchecked arithmetic on `shares` in deposit()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.6,
      "score": 3.36,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 33,
        "end_line": 33,
        "function": "deposit",
        "contract": ""
      },
      "description": "deposit() assigns `ctx.accounts.user_vault.shares = ctx.accounts.user_vault.shares + shares` from an arithmetic expression with no `checked_*`/`Saturating` wrapper. Token amounts are `u64`; in an anchor build with overflow checks on, an overflow panics the instruction (DoS of the account), and anywhere the checks are off - or the value is derived from a cast - it wraps around and produces a tiny or absurd balance.",
      "evidence": "ctx.accounts.user_vault.shares = ctx.accounts.user_vault.shares + shares;",
      "exploit_sketch": [
        "Feed an amount near u64::MAX (multi-token deposits, or a manipulated `limiter` multiplier).",
        "The accumulation wraps; your recorded share count becomes attacker-controlled."
      ],
      "patch_sketch": [
        "`checked_add`/`checked_mul` with a program error on overflow.",
        "Bound instruction inputs (`require!(amount < MAX_DEPOSIT, ...)`) before arithmetic.",
        "Cast through `u128` for intermediates."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-022_sg_sol_022_unchecked_arithmetic_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_022_unchecked_arithmetic_on_shares_in_dep",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-022 - Unchecked arithmetic on `shares` in deposit()\n// Severity: medium (confidence 0.60)\n// Rule    : MATH-002\n// Skill   : skills/solana-math/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:33 (deposit)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_022_unchecked_arithmetic_on_shares_in_dep\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ deposit };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-022 - Unchecked arithmetic on `shares` in deposit()\n#[tokio::test]\nasync fn sg_sol_022_unchecked_arithmetic_on_shares_in_dep() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Feed an amount near u64::MAX (multi-token deposits, or a manipulated `limiter` multiplier).\n    // Step 2 - The accumulation wraps; your recorded share count becomes attacker-controlled.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-022: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_022_unchecked_arithmetic_on_shares_in_dep_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   `checked_add`/`checked_mul` with a program error on overflow.\n    //   Bound instruction inputs (`require!(amount < MAX_DEPOSIT, ...)`) before arithmetic.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-math/SKILL.md",
      "skill_checklist": [
        "Arithmetic uses `checked_add/sub/mul/div` or `Saturating`. If the workspace sets",
        "Intermediates are widened (`u128`) and narrowed only at the end, after bounds checks."
      ],
      "cwe": "CWE-682",
      "tags": [
        "solana",
        "overflow",
        "unchecked-math",
        "MATH-002"
      ],
      "corroborated_by": [
        "ACCT-001 (SG-SOL-012)",
        "ACCT-002 (SG-SOL-006)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-023",
      "detector": "solana_math",
      "rule": "MATH-002",
      "title": "Unchecked arithmetic on `shares` in withdraw()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.6,
      "score": 3.36,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 55,
        "end_line": 55,
        "function": "withdraw",
        "contract": ""
      },
      "description": "withdraw() assigns `user_vault.shares = user_vault.shares - amount` from an arithmetic expression with no `checked_*`/`Saturating` wrapper. Token amounts are `u64`; in an anchor build with overflow checks on, an overflow panics the instruction (DoS of the account), and anywhere the checks are off - or the value is derived from a cast - it wraps around and produces a tiny or absurd balance.",
      "evidence": "user_vault.shares = user_vault.shares - amount;",
      "exploit_sketch": [
        "Feed an amount near u64::MAX (multi-token deposits, or a manipulated `limiter` multiplier).",
        "The accumulation wraps; your recorded share count becomes attacker-controlled."
      ],
      "patch_sketch": [
        "`checked_add`/`checked_mul` with a program error on overflow.",
        "Bound instruction inputs (`require!(amount < MAX_DEPOSIT, ...)`) before arithmetic.",
        "Cast through `u128` for intermediates."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-023_sg_sol_023_unchecked_arithmetic_.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_023_unchecked_arithmetic_on_shares_in_wit",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-023 - Unchecked arithmetic on `shares` in withdraw()\n// Severity: medium (confidence 0.60)\n// Rule    : MATH-002\n// Skill   : skills/solana-math/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:55 (withdraw)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_023_unchecked_arithmetic_on_shares_in_wit\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ withdraw };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-023 - Unchecked arithmetic on `shares` in withdraw()\n#[tokio::test]\nasync fn sg_sol_023_unchecked_arithmetic_on_shares_in_wit() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Feed an amount near u64::MAX (multi-token deposits, or a manipulated `limiter` multiplier).\n    // Step 2 - The accumulation wraps; your recorded share count becomes attacker-controlled.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-023: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_023_unchecked_arithmetic_on_shares_in_wit_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   `checked_add`/`checked_mul` with a program error on overflow.\n    //   Bound instruction inputs (`require!(amount < MAX_DEPOSIT, ...)`) before arithmetic.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-math/SKILL.md",
      "skill_checklist": [
        "Arithmetic uses `checked_add/sub/mul/div` or `Saturating`. If the workspace sets",
        "Intermediates are widened (`u128`) and narrowed only at the end, after bounds checks."
      ],
      "cwe": "CWE-682",
      "tags": [
        "solana",
        "overflow",
        "unchecked-math",
        "MATH-002"
      ],
      "corroborated_by": [
        "AUTH-003 (SG-SOL-021)",
        "SIGNER-002 (SG-SOL-003)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-023",
      "detector": "evm_sig_replay",
      "rule": "SR-004",
      "title": "Raw ecrecover without malleability checks in claimTreasuryGrant()",
      "severity": "medium",
      "severity_weight": 4.0,
      "confidence": 0.6,
      "score": 3.36,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 88,
        "end_line": 88,
        "function": "claimTreasuryGrant",
        "contract": "HyperVault"
      },
      "description": "claimTreasuryGrant() calls `ecrecover` directly. Secp256k1 signatures are malleable: (v, r, s) and (v', r, n-s) both recover the same address. Without a high-s rejection, and without restricting v to {27,28}, the same authorisation appears as two different hashes, which breaks any de-duplication or replay map keyed on the payload.",
      "evidence": "address signer = ecrecover(doc, v, r, s);",
      "exploit_sketch": [
        "Take a used signature, flip s to n - s and adjust v.",
        "If dedupe keys on the raw payload rather than the recovered digest, the second variant passes."
      ],
      "patch_sketch": [
        "Use OpenZeppelin `ECDSA.tryRecover`/`recover` which enforces low-s and v in {27,28}.",
        "Key replay maps on the eip-712 digest, not on the submitted bytes."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-023_sg_evm_023_raw_ecrecover_without.t.sol",
        "how_to_run": "forge test --match-contract SgEvm023RawEcrecoverWithoutMalleabilityCHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-023 - Raw ecrecover without malleability checks in claimTreasuryGrant()\n// Severity: medium (confidence 0.60)\n// Rule    : SR-004\n// Skill   : skills/signature-replay/SKILL.md\n// Location: solidity/HyperVault.sol:88 (claimTreasuryGrant)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm023RawEcrecoverWithoutMalleabilityCHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm023RawEcrecoverWithoutMalleabilityCHarness\n/// @notice PoC harness for SG-EVM-023.\ncontract SgEvm023RawEcrecoverWithoutMalleabilityCHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Raw ecrecover without malleability checks in claimTreasuryGrant()\n    function test_sg_evm_023_raw_ecrecover_without_malleability_ch() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   Take a used signature, flip s to n - s and adjust v.\n        // Step 2 - If dedupe keys on the raw payload rather than the recovered digest, the second variant passes.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_023_raw_ecrecover_without_malleability_ch_afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Use OpenZeppelin `ECDSA.tryRecover`/`recover` which enforces low-s and v in {27,28}.\n        //   Key replay maps on the eip-712 digest, not on the submitted bytes.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/signature-replay/SKILL.md",
      "skill_checklist": [
        "Malleability is handled by a library (`ECDSA.recover`/`tryRecover`, enforced low-s,"
      ],
      "cwe": "CWE-294",
      "tags": [
        "signature",
        "malleability",
        "SR-004"
      ],
      "corroborated_by": [
        "CEI-001 (SG-EVM-003)",
        "EC-001 (SG-EVM-010)",
        "EC-003 (SG-EVM-024)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-SOL-024",
      "detector": "solana_authority",
      "rule": "AUTH-002",
      "title": "`realloc::payer = vault_config` is not a funded signer in Withdraw",
      "severity": "low",
      "severity_weight": 2.0,
      "confidence": 0.6,
      "score": 1.68,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 169,
        "end_line": 169,
        "function": "",
        "contract": ""
      },
      "description": "`Withdraw` resizes an account and charges the rent delta to `vault_config`, which is not declared in this struct. Solana requires the rent payer of a resize to be a writable, funded signer; a PDA or program-owned account cannot pay it, so the resize fails (availability) or - if the slot can be filled by whoever calls - the cost lands on a third party. This is a design note rather than a proof of theft, which is why it is graded low/medium.",
      "evidence": "#[account(mut, seeds = [b\"vault\", vault_config.key().as_ref()], bump = vault_config.bump, realloc = 8, realloc::payer = vault_config, realloc::zero = false)]",
      "exploit_sketch": [
        "Trigger the realloc path repeatedly so the charged account bleeds lamports it never agreed to.",
        "Or front-run the growth so the rent cost lands on the victim's account."
      ],
      "patch_sketch": [
        "`#[account(mut)] payer: Signer<'info>` as `realloc::payer`, verified as the tx fee payer.",
        "Prefer fixed `space` at init over dynamic realloc in value-critical paths.",
        "If the PDA must fund growth, fund it explicitly with a signed transfer first."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-024_sg_sol_024_realloc_payer_vault_c.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_024_realloc_payer_vault_config_is_not_a_f",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-024 - `realloc::payer = vault_config` is not a funded signer in Withdraw\n// Severity: low (confidence 0.60)\n// Rule    : AUTH-002\n// Skill   : skills/solana-account-validation/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:169\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_024_realloc_payer_vault_config_is_not_a_f\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ vulnerableFunction };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-024 - `realloc::payer = vault_config` is not a funded signer in Withdraw\n#[tokio::test]\nasync fn sg_sol_024_realloc_payer_vault_config_is_not_a_f() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Trigger the realloc path repeatedly so the charged account bleeds lamports it never agreed to.\n    // Step 2 - Or front-run the growth so the rent cost lands on the victim's account.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-024: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_024_realloc_payer_vault_config_is_not_a_f_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   `#[account(mut)] payer: Signer<'info>` as `realloc::payer`, verified as the tx fee payer.\n    //   Prefer fixed `space` at init over dynamic realloc in value-critical paths.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-account-validation/SKILL.md",
      "skill_checklist": [
        "SPL accounts assert `owner == token_program`, the right `mint`, and the expected authority"
      ],
      "cwe": "CWE-269",
      "tags": [
        "solana",
        "realloc",
        "authority-note",
        "AUTH-002"
      ],
      "corroborated_by": [
        "ACCT-003 (SG-SOL-019)"
      ],
      "independent_confirmation": 1
    },
    {
      "id": "SG-SOL-025",
      "detector": "solana_math",
      "rule": "MATH-004",
      "title": "Narrowing cast `as u64` on an amount in slash()",
      "severity": "low",
      "severity_weight": 2.0,
      "confidence": 0.5,
      "score": 1.6,
      "chain": "solana",
      "location": {
        "file": "solana/vault/programs/vault/src/lib.rs",
        "line": 67,
        "end_line": 67,
        "function": "slash",
        "contract": ""
      },
      "description": "slash() casts through a narrower integer while computing token amounts. Combined with the multiply/divide ordering above, silent truncation is the second half of the precision bug: the math can be right in `u128` and wrong once cast back.",
      "evidence": "let shares = (report.amount as u64) * vault.limiter;",
      "exploit_sketch": [
        "Choose inputs whose intermediate exceeds the target width.",
        "The truncated value becomes the share/balance the program trusts."
      ],
      "patch_sketch": [
        "Keep intermediates in `u128` and cast only at the end, after bounds checks.",
        "Add a unit test around `u64::MAX / 2` sized amounts."
      ],
      "poc": {
        "language": "rust",
        "filename": "pocs/sg-sol-025_sg_sol_025_narrowing_cast_as_u64.rs",
        "how_to_run": "cargo test --features test-suites sg_sol_025_narrowing_cast_as_u64_on_an_amount_in",
        "code": "// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.\n// Runs against the synthetic sample program only. No mainnet interaction.\n//\n// Finding : SG-SOL-025 - Narrowing cast `as u64` on an amount in slash()\n// Severity: low (confidence 0.50)\n// Rule    : MATH-004\n// Skill   : skills/solana-math/SKILL.md\n// Location: solana/vault/programs/vault/src/lib.rs:67 (slash)\n//\n// Setup:\n//   cargo new poc-harness --lib   # or use the sample's Anchor workspace\n//   add dev-dependencies: solana-program-test = \"1.18\", tokio = { version = \"1\", features = [\"macros\"] }\n//   cargo test --features test-suites sg_sol_025_narrowing_cast_as_u64_on_an_amount_in\n\n#![cfg(feature = \"test-suites\")]\n\nuse anchor_lang::prelude::*;\nuse solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};\n\n// TODO: import the sample program under test, e.g.\n// use vault::instruction::{ slash };\n// use vault::{Accounts, Lib};\n\n/// PoC harness for SG-SOL-025 - Narrowing cast `as u64` on an amount in slash()\n#[tokio::test]\nasync fn sg_sol_025_narrowing_cast_as_u64_on_an_amount_in() {\n    // -- Arrange -------------------------------------------------------------\n    // Step 1 - Choose inputs whose intermediate exceeds the target width.\n    // Step 2 - The truncated value becomes the share/balance the program trusts.\n    // Step 3 - \n    //\n    // let mut program_test = ProgramTest::new(\"vault\", vault::ID, None);\n    // program_test.add_program(...);\n    // let (banks_client, payer, blockhash) = program_test.start().await;\n\n    // -- Act -----------------------------------------------------------------\n    // Build the instruction that omits the validation SolGuardian flagged, e.g.\n    // pass an unsigned / wrong-owner account, or a foreign CPI program id.\n\n    // -- Assert --------------------------------------------------------------\n    // The vulnerable build accepts the instruction:\n    // assert!(result.is_ok(), \"expected the buggy path to succeed (proves the gap)\");\n    panic!(\n        \"PoC stub for SG-SOL-025: wire the instruction builder above. \\\n         SolGuardian emits skeletons, not weapons.\"\n    );\n}\n\n/// Control test: after the patch the same instruction must fail with the account\n/// validation error.\n#[tokio::test]\nasync fn sg_sol_025_narrowing_cast_as_u64_on_an_amount_in_after_patch_fails() {\n    // Patch sketch from the finding:\n    //   Keep intermediates in `u128` and cast only at the end, after bounds checks.\n    //   Add a unit test around `u64::MAX / 2` sized amounts.\n    panic!(\"implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)\");\n}\n"
      },
      "skill": "skills/solana-math/SKILL.md",
      "skill_checklist": [
        "`as` casts are reviewed for truncation (`u128 as u64`, `i64 as u64`).",
        "Multiplication happens **before** division; division-first expressions are precision-loss"
      ],
      "cwe": "CWE-682",
      "tags": [
        "solana",
        "cast",
        "precision",
        "MATH-004"
      ],
      "corroborated_by": [
        "CPI-003 (SG-SOL-015)",
        "SIGNER-002 (SG-SOL-004)"
      ],
      "independent_confirmation": 2
    },
    {
      "id": "SG-EVM-024",
      "detector": "evm_external_calls",
      "rule": "EC-003",
      "title": "Native `.transfer()` gas-stipend transfer in claimTreasuryGrant()",
      "severity": "low",
      "severity_weight": 2.0,
      "confidence": 0.5,
      "score": 1.6,
      "chain": "hyperevm",
      "location": {
        "file": "solidity/HyperVault.sol",
        "line": 90,
        "end_line": 90,
        "function": "claimTreasuryGrant",
        "contract": "HyperVault"
      },
      "description": "claimTreasuryGrant() forwards value with `.transfer/send()`, which forwards exactly 2300 gas. That is enough for a plain wallet but not for most smart-contract recipients, so payments to contracts (multisigs, other vaults, account-abstraction wallets) fail - and on HyperEVM the stipend assumption has changed more than once. Reported as low because it is availability, not theft.",
      "evidence": "payable(msg.sender).transfer(amount);",
      "exploit_sketch": [
        "No attacker needed: legitimate users with contract wallets simply cannot be paid.",
        "If the result is unchecked, the contract marks the payout as done anyway."
      ],
      "patch_sketch": [
        "Prefer withdrawal patterns the user triggers, with `Address.sendValue` + checked result.",
        "Never assume 2300 gas is enough for every recipient."
      ],
      "poc": {
        "language": "solidity",
        "filename": "pocs/sg-evm-024_sg_evm_024_native_transfer_gas_s.t.sol",
        "how_to_run": "forge test --match-contract SgEvm024NativeTransferGasStipendTransferHarness -vvvv",
        "code": "// SPDX-License-Identifier: MIT\n// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.\n// This is a skeleton: it names the vulnerable function and the shape of the attack,\n// but contains no live mainnet exploit path. Run it against the sample contract in a\n// local Foundry fork/anvil sandbox, never against deployed funds.\n//\n// Finding : SG-EVM-024 - Native `.transfer()` gas-stipend transfer in claimTreasuryGrant()\n// Severity: low (confidence 0.50)\n// Rule    : EC-003\n// Skill   : skills/external-calls/SKILL.md\n// Location: solidity/HyperVault.sol:90 (claimTreasuryGrant)\n//\n// Setup:\n//   forge init poc-harness && cp -r samples/solidity .\n//   forge test --match-contract SgEvm024NativeTransferGasStipendTransferHarness -vvvv\n\npragma solidity ^0.8.24;\n\nimport \"forge-std/Test.sol\";\nimport \"../../samples/solidity/HyperVault.sol\";\n\n/// @title SgEvm024NativeTransferGasStipendTransferHarness\n/// @notice PoC harness for SG-EVM-024.\ncontract SgEvm024NativeTransferGasStipendTransferHarness is Test {\n    HyperVault internal target;\n    address internal attacker;\n    uint256 internal constant SEED_ETH = 2 ether;\n\n    function setUp() public {\n        target = new HyperVault();\n        attacker = makeAddr(\"attacker\");\n        vm.deal(attacker, SEED_ETH);\n        // TODO: fund / seed the vault so the vulnerable path is reachable\n    }\n\n    /// @notice Native `.transfer()` gas-stipend transfer in claimTreasuryGrant()\n    function test_sg_evm_024_native_transfer_gas_stipend_transfer_() public {\n        // Step 1 - attacker reaches the vulnerable entry point:\n        //   No attacker needed: legitimate users with contract wallets simply cannot be paid.\n        // Step 2 - If the result is unchecked, the contract marks the payout as done anyway.\n        // Step 3 - \n\n        vm.prank(attacker);\n        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.\n\n        // Assertion that proves the bug (fails once patched):\n        // assertGt(attacker.balance, SEED_ETH, \"expected value to be drained twice\");\n        assertTrue(true, \"placeholder: wire the real call above\");\n    }\n\n    /// @notice Control test: the same path must revert / not profit after the fix.\n    function test_sg_evm_024_native_transfer_gas_stipend_transfer__afterPatch_reverts() public {\n        // Patch sketch from the finding:\n        //   Prefer withdrawal patterns the user triggers, with `Address.sendValue` + checked result.\n        //   Never assume 2300 gas is enough for every recipient.\n        assertTrue(true, \"implement once the patch lands\");\n    }\n}\n"
      },
      "skill": "skills/external-calls/SKILL.md",
      "skill_checklist": [
        "The 2300-gas stipend of `send`/`transfer` is acceptable for every plausible recipient"
      ],
      "cwe": "CWE-252",
      "tags": [
        "gas-stipend",
        "availability",
        "EC-003"
      ],
      "corroborated_by": [
        "CEI-001 (SG-EVM-003)",
        "SR-001 (SG-EVM-017)",
        "SR-002 (SG-EVM-002)",
        "SR-003 (SG-EVM-020)",
        "SR-004 (SG-EVM-023)"
      ],
      "independent_confirmation": 2
    }
  ],
  "orchestration": {
    "model": "parallel specialist agents -> adjudicator -> report writer",
    "workers": 5,
    "agent_runs": [
      {
        "agent": "evm-hunter#1",
        "files_scanned": 1,
        "findings": 0,
        "duration_ms": 214,
        "log": [
          "evm-hunter#1: 1 file(s), 7 detector(s) [evm_access, evm_delegatecall, evm_external_calls, evm_oracle, evm_reentrancy, evm_selfdestruct, evm_sig_replay]",
          "evm-hunter#1: done, 0 finding(s) in 214 ms"
        ]
      },
      {
        "agent": "solana-hunter#1",
        "files_scanned": 1,
        "findings": 0,
        "duration_ms": 21,
        "log": [
          "solana-hunter#1: 1 file(s), 6 detector(s) [solana_accounts, solana_authority, solana_cpi, solana_deser, solana_math, solana_signer]",
          "solana-hunter#1: done, 0 finding(s) in 21 ms"
        ]
      },
      {
        "agent": "evm-hunter#2",
        "files_scanned": 1,
        "findings": 10,
        "duration_ms": 513,
        "log": [
          "evm-hunter#2: 1 file(s), 7 detector(s) [evm_access, evm_delegatecall, evm_external_calls, evm_oracle, evm_reentrancy, evm_selfdestruct, evm_sig_replay]",
          "  evm_access             solidity/Vault.sol -> 4 finding(s) (303 ms)",
          "  evm_external_calls     solidity/Vault.sol -> 1 finding(s) (186 ms)",
          "  evm_oracle             solidity/Vault.sol -> 2 finding(s) (7 ms)",
          "  evm_reentrancy         solidity/Vault.sol -> 2 finding(s) (6 ms)",
          "  evm_selfdestruct       solidity/Vault.sol -> 1 finding(s) (4 ms)",
          "evm-hunter#2: done, 10 finding(s) in 513 ms"
        ]
      },
      {
        "agent": "evm-hunter#3",
        "files_scanned": 1,
        "findings": 14,
        "duration_ms": 437,
        "log": [
          "evm-hunter#3: 1 file(s), 7 detector(s) [evm_access, evm_delegatecall, evm_external_calls, evm_oracle, evm_reentrancy, evm_selfdestruct, evm_sig_replay]",
          "  evm_access             solidity/HyperVault.sol -> 1 finding(s) (73 ms)",
          "  evm_delegatecall       solidity/HyperVault.sol -> 2 finding(s) (4 ms)",
          "  evm_external_calls     solidity/HyperVault.sol -> 2 finding(s) (316 ms)",
          "  evm_oracle             solidity/HyperVault.sol -> 1 finding(s) (20 ms)",
          "  evm_reentrancy         solidity/HyperVault.sol -> 1 finding(s) (5 ms)",
          "  evm_selfdestruct       solidity/HyperVault.sol -> 3 finding(s) (6 ms)",
          "  evm_sig_replay         solidity/HyperVault.sol -> 4 finding(s) (11 ms)",
          "evm-hunter#3: done, 14 finding(s) in 437 ms"
        ]
      },
      {
        "agent": "solana-hunter#2",
        "files_scanned": 1,
        "findings": 25,
        "duration_ms": 397,
        "log": [
          "solana-hunter#2: 1 file(s), 6 detector(s) [solana_accounts, solana_authority, solana_cpi, solana_deser, solana_math, solana_signer]",
          "  solana_accounts        solana/vault/programs/vault/src/lib.rs -> 6 finding(s) (357 ms)",
          "  solana_authority       solana/vault/programs/vault/src/lib.rs -> 3 finding(s) (10 ms)",
          "  solana_cpi             solana/vault/programs/vault/src/lib.rs -> 4 finding(s) (8 ms)",
          "  solana_deser           solana/vault/programs/vault/src/lib.rs -> 1 finding(s) (3 ms)",
          "  solana_math            solana/vault/programs/vault/src/lib.rs -> 5 finding(s) (12 ms)",
          "  solana_signer          solana/vault/programs/vault/src/lib.rs -> 6 finding(s) (3 ms)",
          "solana-hunter#2: done, 25 finding(s) in 397 ms"
        ]
      },
      {
        "agent": "adjudicator",
        "files_scanned": 0,
        "findings": 49,
        "duration_ms": 0,
        "log": [
          "cross-checking 49 finding(s) from 13 independent detector pass(es)",
          "30 finding(s) independently confirmed by another detector",
          "best-corroborated site: solidity/HyperVault.sol:87 (claimTreasuryGrant) <- 3 rules",
          "7 site(s) where detectors disagree on severity (kept as separate findings, not averaged)"
        ]
      },
      {
        "agent": "report-writer",
        "files_scanned": 0,
        "findings": 49,
        "duration_ms": 0,
        "log": [
          "validating 49 finding(s) against the output contract",
          "severity distribution: critical=16, high=18, medium=12, low=3",
          "contract check: 49/49 complete",
          "all findings resolve to a skill pack under skills/"
        ]
      }
    ],
    "corroborated_findings": 30,
    "note": "hunters are sharded by file and run concurrently; the adjudicator only annotates corroboration and never changes a grade"
  },
  "demo_metrics": {
    "seeded_total": 17,
    "caught": 17,
    "recall": 1.0,
    "missed": [],
    "unseeded_findings": [
      "SG-EVM-003",
      "SG-EVM-007",
      "SG-SOL-002",
      "SG-SOL-004",
      "SG-SOL-005",
      "SG-SOL-007",
      "SG-EVM-009",
      "SG-SOL-008",
      "SG-SOL-009",
      "SG-EVM-010",
      "SG-EVM-013",
      "SG-EVM-014",
      "SG-EVM-015",
      "SG-SOL-013",
      "SG-SOL-014",
      "SG-EVM-017",
      "SG-EVM-019",
      "SG-SOL-015",
      "SG-SOL-016",
      "SG-SOL-018",
      "SG-SOL-019",
      "SG-SOL-020",
      "SG-EVM-020",
      "SG-EVM-021",
      "SG-SOL-021",
      "SG-EVM-022",
      "SG-SOL-022",
      "SG-SOL-023",
      "SG-EVM-023",
      "SG-SOL-024",
      "SG-SOL-025",
      "SG-EVM-024"
    ],
    "detail": [
      {
        "key": "evm-reentrancy-withdraw",
        "caught": true,
        "finding_id": "SG-EVM-005",
        "rule": "CEI-001",
        "detector": "evm_reentrancy",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "state updated after the value-bearing external call"
      },
      {
        "key": "evm-access-adminDrain",
        "caught": true,
        "finding_id": "SG-EVM-006",
        "rule": "AC-001",
        "detector": "evm_access",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "anyone can empty the vault: no modifier, no sender check"
      },
      {
        "key": "evm-tx-origin-setOperator",
        "caught": true,
        "finding_id": "SG-EVM-011",
        "rule": "AC-002",
        "detector": "evm_access",
        "severity_expected": "high",
        "severity_found": "high",
        "note": "authorisation based on tx.origin"
      },
      {
        "key": "evm-unchecked-call-refundPending",
        "caught": true,
        "finding_id": "SG-EVM-012",
        "rule": "EC-001",
        "detector": "evm_external_calls",
        "severity_expected": "high",
        "severity_found": "high",
        "note": "ERC20 transfer() return value discarded"
      },
      {
        "key": "evm-spot-price-assetValue",
        "caught": true,
        "finding_id": "SG-EVM-008",
        "rule": "OR-001",
        "detector": "evm_oracle",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "AMM reserves + unchecked Chainlink round decide valuation"
      },
      {
        "key": "hyperevm-delegatecall-migrate",
        "caught": true,
        "finding_id": "SG-EVM-001",
        "rule": "DC-001",
        "detector": "evm_delegatecall",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "delegatecall into a caller-chosen target"
      },
      {
        "key": "hyperevm-sig-replay-claimTreasuryGrant",
        "caught": true,
        "finding_id": "SG-EVM-002",
        "rule": "SR-002",
        "detector": "evm_sig_replay",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "no nonce, no deadline, no domain binding, raw ecrecover"
      },
      {
        "key": "hyperevm-selfdestruct-decommission",
        "caught": true,
        "finding_id": "SG-EVM-004",
        "rule": "SD-001",
        "detector": "evm_selfdestruct",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "reachable selfdestruct with a caller-chosen beneficiary"
      },
      {
        "key": "hyperevm-stuck-funds-recoverTokens",
        "caught": true,
        "finding_id": "SG-EVM-018",
        "rule": "SF-001",
        "detector": "evm_selfdestruct",
        "severity_expected": "high",
        "severity_found": "high",
        "note": "recovery gated on pendingAdmin, which is never assigned"
      },
      {
        "key": "hyperevm-unguarded-logic-pointer",
        "caught": true,
        "finding_id": "SG-EVM-016",
        "rule": "DC-002",
        "detector": "evm_delegatecall",
        "severity_expected": "high",
        "severity_found": "high",
        "note": "setImplementation() writable by anyone"
      },
      {
        "key": "solana-missing-signer-withdraw",
        "caught": true,
        "finding_id": "SG-SOL-003",
        "rule": "SIGNER-002",
        "detector": "solana_signer",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "user_authority is a raw AccountInfo used for a lamport credit"
      },
      {
        "key": "solana-token-cpi-unvalidated-deposit",
        "caught": true,
        "finding_id": "SG-SOL-006",
        "rule": "ACCT-002",
        "detector": "solana_accounts",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "user_token_account handed to token::transfer with no owner/mint validation"
      },
      {
        "key": "solana-account-confusion-deposit",
        "caught": true,
        "finding_id": "SG-SOL-012",
        "rule": "ACCT-001",
        "detector": "solana_accounts",
        "severity_expected": "high",
        "severity_found": "high",
        "note": "user_vault is an UncheckedAccount carrying a stored share balance"
      },
      {
        "key": "solana-unchecked-cpi-settle_external",
        "caught": true,
        "finding_id": "SG-SOL-011",
        "rule": "CPI-001",
        "detector": "solana_cpi",
        "severity_expected": "high",
        "severity_found": "high",
        "note": "program::invoke result dropped; callee id comes from the client"
      },
      {
        "key": "solana-precision-loss-deposit",
        "caught": true,
        "finding_id": "SG-SOL-010",
        "rule": "MATH-001",
        "detector": "solana_math",
        "severity_expected": "high",
        "severity_found": "high",
        "note": "amount / total_deposits * SCALE truncates small deposits to zero shares"
      },
      {
        "key": "solana-unsafe-deser-load_foreign_config",
        "caught": true,
        "finding_id": "SG-SOL-001",
        "rule": "DESER-001",
        "detector": "solana_deser",
        "severity_expected": "critical",
        "severity_found": "critical",
        "note": "raw pointer cast over foreign account data"
      },
      {
        "key": "solana-remaining-accounts-settle_many",
        "caught": true,
        "finding_id": "SG-SOL-017",
        "rule": "REMOT-001",
        "detector": "solana_cpi",
        "severity_expected": "medium",
        "severity_found": "medium",
        "note": "remaining_accounts indexed before any length check"
      }
    ]
  },
  "agent_runs": [
    {
      "agent": "evm-hunter#1",
      "files_scanned": 1,
      "findings": 0,
      "duration_ms": 214,
      "log": [
        "evm-hunter#1: 1 file(s), 7 detector(s) [evm_access, evm_delegatecall, evm_external_calls, evm_oracle, evm_reentrancy, evm_selfdestruct, evm_sig_replay]",
        "evm-hunter#1: done, 0 finding(s) in 214 ms"
      ]
    },
    {
      "agent": "solana-hunter#1",
      "files_scanned": 1,
      "findings": 0,
      "duration_ms": 21,
      "log": [
        "solana-hunter#1: 1 file(s), 6 detector(s) [solana_accounts, solana_authority, solana_cpi, solana_deser, solana_math, solana_signer]",
        "solana-hunter#1: done, 0 finding(s) in 21 ms"
      ]
    },
    {
      "agent": "evm-hunter#2",
      "files_scanned": 1,
      "findings": 10,
      "duration_ms": 513,
      "log": [
        "evm-hunter#2: 1 file(s), 7 detector(s) [evm_access, evm_delegatecall, evm_external_calls, evm_oracle, evm_reentrancy, evm_selfdestruct, evm_sig_replay]",
        "  evm_access             solidity/Vault.sol -> 4 finding(s) (303 ms)",
        "  evm_external_calls     solidity/Vault.sol -> 1 finding(s) (186 ms)",
        "  evm_oracle             solidity/Vault.sol -> 2 finding(s) (7 ms)",
        "  evm_reentrancy         solidity/Vault.sol -> 2 finding(s) (6 ms)",
        "  evm_selfdestruct       solidity/Vault.sol -> 1 finding(s) (4 ms)",
        "evm-hunter#2: done, 10 finding(s) in 513 ms"
      ]
    },
    {
      "agent": "evm-hunter#3",
      "files_scanned": 1,
      "findings": 14,
      "duration_ms": 437,
      "log": [
        "evm-hunter#3: 1 file(s), 7 detector(s) [evm_access, evm_delegatecall, evm_external_calls, evm_oracle, evm_reentrancy, evm_selfdestruct, evm_sig_replay]",
        "  evm_access             solidity/HyperVault.sol -> 1 finding(s) (73 ms)",
        "  evm_delegatecall       solidity/HyperVault.sol -> 2 finding(s) (4 ms)",
        "  evm_external_calls     solidity/HyperVault.sol -> 2 finding(s) (316 ms)",
        "  evm_oracle             solidity/HyperVault.sol -> 1 finding(s) (20 ms)",
        "  evm_reentrancy         solidity/HyperVault.sol -> 1 finding(s) (5 ms)",
        "  evm_selfdestruct       solidity/HyperVault.sol -> 3 finding(s) (6 ms)",
        "  evm_sig_replay         solidity/HyperVault.sol -> 4 finding(s) (11 ms)",
        "evm-hunter#3: done, 14 finding(s) in 437 ms"
      ]
    },
    {
      "agent": "solana-hunter#2",
      "files_scanned": 1,
      "findings": 25,
      "duration_ms": 397,
      "log": [
        "solana-hunter#2: 1 file(s), 6 detector(s) [solana_accounts, solana_authority, solana_cpi, solana_deser, solana_math, solana_signer]",
        "  solana_accounts        solana/vault/programs/vault/src/lib.rs -> 6 finding(s) (357 ms)",
        "  solana_authority       solana/vault/programs/vault/src/lib.rs -> 3 finding(s) (10 ms)",
        "  solana_cpi             solana/vault/programs/vault/src/lib.rs -> 4 finding(s) (8 ms)",
        "  solana_deser           solana/vault/programs/vault/src/lib.rs -> 1 finding(s) (3 ms)",
        "  solana_math            solana/vault/programs/vault/src/lib.rs -> 5 finding(s) (12 ms)",
        "  solana_signer          solana/vault/programs/vault/src/lib.rs -> 6 finding(s) (3 ms)",
        "solana-hunter#2: done, 25 finding(s) in 397 ms"
      ]
    },
    {
      "agent": "adjudicator",
      "files_scanned": 0,
      "findings": 49,
      "duration_ms": 0,
      "log": [
        "cross-checking 49 finding(s) from 13 independent detector pass(es)",
        "30 finding(s) independently confirmed by another detector",
        "best-corroborated site: solidity/HyperVault.sol:87 (claimTreasuryGrant) <- 3 rules",
        "7 site(s) where detectors disagree on severity (kept as separate findings, not averaged)"
      ]
    },
    {
      "agent": "report-writer",
      "files_scanned": 0,
      "findings": 49,
      "duration_ms": 0,
      "log": [
        "validating 49 finding(s) against the output contract",
        "severity distribution: critical=16, high=18, medium=12, low=3",
        "contract check: 49/49 complete",
        "all findings resolve to a skill pack under skills/"
      ]
    }
  ]
}
