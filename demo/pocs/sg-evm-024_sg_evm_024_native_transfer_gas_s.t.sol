// SPDX-License-Identifier: MIT
// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.
// This is a skeleton: it names the vulnerable function and the shape of the attack,
// but contains no live mainnet exploit path. Run it against the sample contract in a
// local Foundry fork/anvil sandbox, never against deployed funds.
//
// Finding : SG-EVM-024 - Native `.transfer()` gas-stipend transfer in claimTreasuryGrant()
// Severity: low (confidence 0.50)
// Rule    : EC-003
// Skill   : skills/external-calls/SKILL.md
// Location: solidity/HyperVault.sol:90 (claimTreasuryGrant)
//
// Setup:
//   forge init poc-harness && cp -r samples/solidity .
//   forge test --match-contract SgEvm024NativeTransferGasStipendTransferHarness -vvvv

pragma solidity ^0.8.24;

import "forge-std/Test.sol";
import "../../samples/solidity/HyperVault.sol";

/// @title SgEvm024NativeTransferGasStipendTransferHarness
/// @notice PoC harness for SG-EVM-024.
contract SgEvm024NativeTransferGasStipendTransferHarness is Test {
    HyperVault internal target;
    address internal attacker;
    uint256 internal constant SEED_ETH = 2 ether;

    function setUp() public {
        target = new HyperVault();
        attacker = makeAddr("attacker");
        vm.deal(attacker, SEED_ETH);
        // TODO: fund / seed the vault so the vulnerable path is reachable
    }

    /// @notice Native `.transfer()` gas-stipend transfer in claimTreasuryGrant()
    function test_sg_evm_024_native_transfer_gas_stipend_transfer_() public {
        // Step 1 - attacker reaches the vulnerable entry point:
        //   No attacker needed: legitimate users with contract wallets simply cannot be paid.
        // Step 2 - If the result is unchecked, the contract marks the payout as done anyway.
        // Step 3 - 

        vm.prank(attacker);
        // TODO: call HyperVault.claimTreasuryGrant(...) with attacker-controlled args.

        // Assertion that proves the bug (fails once patched):
        // assertGt(attacker.balance, SEED_ETH, "expected value to be drained twice");
        assertTrue(true, "placeholder: wire the real call above");
    }

    /// @notice Control test: the same path must revert / not profit after the fix.
    function test_sg_evm_024_native_transfer_gas_stipend_transfer__afterPatch_reverts() public {
        // Patch sketch from the finding:
        //   Prefer withdrawal patterns the user triggers, with `Address.sendValue` + checked result.
        //   Never assume 2300 gas is enough for every recipient.
        assertTrue(true, "implement once the patch lands");
    }
}
