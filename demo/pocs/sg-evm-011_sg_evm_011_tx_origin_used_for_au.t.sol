// SPDX-License-Identifier: MIT
// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.
// This is a skeleton: it names the vulnerable function and the shape of the attack,
// but contains no live mainnet exploit path. Run it against the sample contract in a
// local Foundry fork/anvil sandbox, never against deployed funds.
//
// Finding : SG-EVM-011 - tx.origin used for authorisation in setOperator()
// Severity: high (confidence 0.85)
// Rule    : AC-002
// Skill   : skills/access-control/SKILL.md
// Location: solidity/Vault.sol:94 (setOperator)
//
// Setup:
//   forge init poc-harness && cp -r samples/solidity .
//   forge test --match-contract SgEvm011TxOriginUsedForAuthorisationInSeHarness -vvvv

pragma solidity ^0.8.24;

import "forge-std/Test.sol";
import "../../samples/solidity/Vault.sol";

/// @title SgEvm011TxOriginUsedForAuthorisationInSeHarness
/// @notice PoC harness for SG-EVM-011.
contract SgEvm011TxOriginUsedForAuthorisationInSeHarness is Test {
    Vault internal target;
    address internal attacker;
    uint256 internal constant SEED_ETH = 2 ether;

    function setUp() public {
        target = new Vault();
        attacker = makeAddr("attacker");
        vm.deal(attacker, SEED_ETH);
        // TODO: fund / seed the vault so the vulnerable path is reachable
    }

    /// @notice tx.origin used for authorisation in setOperator()
    function test_sg_evm_011_tx_origin_used_for_authorisation_in_s() public {
        // Step 1 - attacker reaches the vulnerable entry point:
        //   Lure the authorised EOA into calling a contract you control (a small incentive is enough).
        // Step 2 - From that contract's fallback, call setOperator(): `tx.origin == owner` still holds.
        // Step 3 - Authorisation is granted while `msg.sender` is your contract.

        vm.prank(attacker);
        // TODO: call Vault.setOperator(...) with attacker-controlled args.

        // Assertion that proves the bug (fails once patched):
        // assertGt(attacker.balance, SEED_ETH, "expected value to be drained twice");
        assertTrue(true, "placeholder: wire the real call above");
    }

    /// @notice Control test: the same path must revert / not profit after the fix.
    function test_sg_evm_011_tx_origin_used_for_authorisation_in_s_afterPatch_reverts() public {
        // Patch sketch from the finding:
        //   Compare against `msg.sender`.
        //   If anti-contract phishing resistance is genuinely wanted, add `require(tx.origin == msg.sender)` *in addition to* the sender check, never instead of it.
        assertTrue(true, "implement once the patch lands");
    }
}
