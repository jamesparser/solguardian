// SPDX-License-Identifier: MIT
// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.
// This is a skeleton: it names the vulnerable function and the shape of the attack,
// but contains no live mainnet exploit path. Run it against the sample contract in a
// local Foundry fork/anvil sandbox, never against deployed funds.
//
// Finding : SG-EVM-004 - Reachable `selfdestruct` with caller-chosen beneficiary in decommission()
// Severity: critical (confidence 0.90)
// Rule    : SD-001
// Skill   : skills/fund-recovery/SKILL.md
// Location: solidity/HyperVault.sol:102 (decommission)
//
// Setup:
//   forge init poc-harness && cp -r samples/solidity .
//   forge test --match-contract SgEvm004ReachableSelfdestructWithCallerCHarness -vvvv

pragma solidity ^0.8.24;

import "forge-std/Test.sol";
import "../../samples/solidity/HyperVault.sol";

/// @title SgEvm004ReachableSelfdestructWithCallerCHarness
/// @notice PoC harness for SG-EVM-004.
contract SgEvm004ReachableSelfdestructWithCallerCHarness is Test {
    HyperVault internal target;
    address internal attacker;
    uint256 internal constant SEED_ETH = 2 ether;

    function setUp() public {
        target = new HyperVault();
        attacker = makeAddr("attacker");
        vm.deal(attacker, SEED_ETH);
        // TODO: fund / seed the vault so the vulnerable path is reachable
    }

    /// @notice Reachable `selfdestruct` with caller-chosen beneficiary in decommission()
    function test_sg_evm_004_reachable_selfdestruct_with_caller_ch() public {
        // Step 1 - attacker reaches the vulnerable entry point:
        //   Call decommission() (directly, or through the unguarded entry that reaches it).
        // Step 2 - The contract's storage is wiped and its ETH balance is pushed to the chosen beneficiary.
        // Step 3 - Every user balance recorded in the contract becomes unrecoverable.

        vm.prank(attacker);
        // TODO: call HyperVault.decommission(...) with attacker-controlled args.

        // Assertion that proves the bug (fails once patched):
        // assertGt(attacker.balance, SEED_ETH, "expected value to be drained twice");
        assertTrue(true, "placeholder: wire the real call above");
    }

    /// @notice Control test: the same path must revert / not profit after the fix.
    function test_sg_evm_004_reachable_selfdestruct_with_caller_ch_afterPatch_reverts() public {
        // Patch sketch from the finding:
        //   Remove selfdestruct from application code; use a pause flag plus an explicit withdrawal route.
        //   If a migration really is needed, gate it behind a timelock + multi-sig governance role and a fixed beneficiary.
        assertTrue(true, "implement once the patch lands");
    }
}
