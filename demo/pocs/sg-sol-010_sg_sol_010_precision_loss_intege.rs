// Educational PoC stub generated by SolGuardian for a Solana / Anchor finding.
// Runs against the synthetic sample program only. No mainnet interaction.
//
// Finding : SG-SOL-010 - Precision loss: integer division before multiplication in deposit()
// Severity: high (confidence 0.85)
// Rule    : MATH-001
// Skill   : skills/solana-math/SKILL.md
// Location: solana/vault/programs/vault/src/lib.rs:31 (deposit)
//
// Setup:
//   cargo new poc-harness --lib   # or use the sample's Anchor workspace
//   add dev-dependencies: solana-program-test = "1.18", tokio = { version = "1", features = ["macros"] }
//   cargo test --features test-suites sg_sol_010_precision_loss_integer_division_befor

#![cfg(feature = "test-suites")]

use anchor_lang::prelude::*;
use solana_program_test::{processor, tokio, ProgramTest, ProgramBanks};

// TODO: import the sample program under test, e.g.
// use vault::instruction::{ deposit };
// use vault::{Accounts, Lib};

/// PoC harness for SG-SOL-010 - Precision loss: integer division before multiplication in deposit()
#[tokio::test]
async fn sg_sol_010_precision_loss_integer_division_befor() {
    // -- Arrange -------------------------------------------------------------
    // Step 1 - Deposit an amount smaller than the divisor (`total_deposits` here) and receive 0 shares.
    // Step 2 - Do it repeatedly: the pool's recorded deposits grow while your share balance stays at zero - or, inverted, you can take shares that were never paid for.
    // Step 3 - Round the other way at withdrawal to extract the residual dust on every user.
    //
    // let mut program_test = ProgramTest::new("vault", vault::ID, None);
    // program_test.add_program(...);
    // let (banks_client, payer, blockhash) = program_test.start().await;

    // -- Act -----------------------------------------------------------------
    // Build the instruction that omits the validation SolGuardian flagged, e.g.
    // pass an unsigned / wrong-owner account, or a foreign CPI program id.

    // -- Assert --------------------------------------------------------------
    // The vulnerable build accepts the instruction:
    // assert!(result.is_ok(), "expected the buggy path to succeed (proves the gap)");
    panic!(
        "PoC stub for SG-SOL-010: wire the instruction builder above. \
         SolGuardian emits skeletons, not weapons."
    );
}

/// Control test: after the patch the same instruction must fail with the account
/// validation error.
#[tokio::test]
async fn sg_sol_010_precision_loss_integer_division_befor_after_patch_fails() {
    // Patch sketch from the finding:
    //   Multiply first, divide last: `let shares = amount.checked_mul(SCALE)?.checked_div(total)?;`
    //   Use `u128` intermediates (`(amount as u128 * SCALE / total) as u64`).
    panic!("implement once the fix lands: expect Err(ProgramError::MissingRequiredSignature)");
}
