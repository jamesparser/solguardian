// SPDX-License-Identifier: MIT
// Educational PoC stub generated by SolGuardian. Target: synthetic samples only.
// This is a skeleton: it names the vulnerable function and the shape of the attack,
// but contains no live mainnet exploit path. Run it against the sample contract in a
// local Foundry fork/anvil sandbox, never against deployed funds.
//
// Finding : SG-EVM-008 - AMM spot price used as valuation in assetValue()
// Severity: critical (confidence 0.88)
// Rule    : OR-001
// Skill   : skills/oracle-price/SKILL.md
// Location: solidity/Vault.sol:120 (assetValue)
//
// Setup:
//   forge init poc-harness && cp -r samples/solidity .
//   forge test --match-contract SgEvm008AmmSpotPriceUsedAsValuationInAssHarness -vvvv

pragma solidity ^0.8.24;

import "forge-std/Test.sol";
import "../../samples/solidity/Vault.sol";

/// @title SgEvm008AmmSpotPriceUsedAsValuationInAssHarness
/// @notice PoC harness for SG-EVM-008.
contract SgEvm008AmmSpotPriceUsedAsValuationInAssHarness is Test {
    Vault internal target;
    address internal attacker;
    uint256 internal constant SEED_ETH = 2 ether;

    function setUp() public {
        target = new Vault();
        attacker = makeAddr("attacker");
        vm.deal(attacker, SEED_ETH);
        // TODO: fund / seed the vault so the vulnerable path is reachable
    }

    /// @notice AMM spot price used as valuation in assetValue()
    function test_sg_evm_008_amm_spot_price_used_as_valuation_in_a() public {
        // Step 1 - attacker reaches the vulnerable entry point:
        //   Flash-loan a large amount of one side of the pool.
        // Step 2 - Swap it into the pool to move reserves (and therefore the printed price) in the same tx.
        // Step 3 - Call assetValue() - the inflated price is now the contract's truth - then mint/borrow/liquidate.

        vm.prank(attacker);
        // TODO: call Vault.assetValue(...) with attacker-controlled args.

        // Assertion that proves the bug (fails once patched):
        // assertGt(attacker.balance, SEED_ETH, "expected value to be drained twice");
        assertTrue(true, "placeholder: wire the real call above");
    }

    /// @notice Control test: the same path must revert / not profit after the fix.
    function test_sg_evm_008_amm_spot_price_used_as_valuation_in_a_afterPatch_reverts() public {
        // Patch sketch from the finding:
        //   Use a time-weighted price (Chainlink TWAP or a multi-block Uniswap V2 `cumulativePrice` window).
        //   Never let an in-block reserve ratio set collateral or payout values.
        assertTrue(true, "implement once the patch lands");
    }
}
